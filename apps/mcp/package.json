{
    "name": "@bot/mcp",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "description": "Read-only MCP server exposing trade DB + backtest engine as tools. Structurally isolated from apps/engine (ADR 0033) and bound to mcp_reader DB role (ADR 0034).",
    "main": "dist/main.js",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "start": "node dist/main.js",
        "lint": "eslint .",
        "typecheck": "tsc --noEmit",
        "test": "jest"
    },
    "dependencies": {
        "@bot/shared": "workspace:^",
        "@bot/analysis": "workspace:^",
        "@modelcontextprotocol/sdk": "^1.29.0",
        "zod": "^3.23.8"
    },
    "devDependencies": {
        "@types/jest": "29.5.14",
        "@types/node": "22.19.19",
        "jest": "29.7.0",
        "ts-jest": "29.2.5",
        "typescript": "5.9.3"
    }
}
