# Milestone log

Per-milestone forensics. Read the specific `M<N>.md` only when debugging a regression or asking "why was this built?"

For the current milestone plan, see [`docs/plans/`](plans/README.md). For deferred items, see [tech-debt.md](tech-debt.md).

| ID | Outcome |
|----|---------|
| M54 | [M54 — xmom entry geometry: arm against the EXPECTED fill price (correctness/fee-bleed, NOT edge) (DONE):](archive/M54.md) Anchors xmom's signal-time SL/TP arm to an expected-fill price `F_exp = P0 × (1 + halfSpread/100)` instead of the raw signal `P0`, plus a pre-send order-size-aware thin-book skip — fixes the open-time anchor mismatch that made the ADR 0045 fill-acceptance guard systematically `force_close` adverse-slippage fills (EXP-022: force_close set averaged R:R-at-fill 1.03, all under the 1.5 guard floor; 67% scheduled force_close rate). **Not an edge fix** — EXP-021/022 already proved no cost-surviving edge; success is force_close-rate + fee-churn reduction only. **D1 (shipped, no-op default):** `xmom_expected_fill_enabled` (boolean, default false) + `xmom_max_depth_fraction` (number\|null, default null) added to `momentumParamsSchema.ts`, with a Zod `.superRefine` making it structurally impossible to enable the anchor without a finite skip budget. **D2 (shipped, no-op default):** `MomentumOrchestratorService.buildMomentumOpenIntent` arms off `F_exp` via new `resolveExpectedFillPrice`; adds fail-closed pre-send depth-budget skip (`isDepthBudgetExceeded`); `referencePrice`/`midAtTrigger` stay anchored to `P0` (protects M48 slFloor + M52 `atrUnitsDrift` breaker); M52 retry rebuild inherits both automatically. **D3 (shipped):** offline replay extended with `--expected-fill`/`--max-depth-fraction`; registered **EXP-025** (renumbered from the plan's assumed EXP-023, which collided with work already allocated 2026-07-08); decision-grade read PENDING paper soak. **D4 (shipped):** 29 new adversarial tests (3 spec files, all 11 plan §9 items) + 2 pre-existing spec files fixed (fixture gap, not a production bug). **Tests:** 4359 total passing, 0 failures. **Review:** 1 round, 4 reviewers (security/clean-code/logic/quant-mandatory): zero blockers; 1 HIGH (clean-code arg-count) + 3 MEDIUMs (clean-code magic numbers, logic + quant doc gaps) all fixed; 2 items carried to tech-debt (pre-existing hardcoded DB credential in `xmom_tp_ratio_replay.mjs` — HIGH; `buildMomentumOpenIntent` size/SRP — LOW). **ADRs:** 0047 amended (new §7); 0045/0051/0004 §6a referenced (not amended). **Deploy:** ships default-off, no migration, no `.env.example` change, xmom stays PAPER-only; enablement is a future soak-gated decision. |
| M53 | [M53 — xmom take-profit arm headroom (decouple arm ratio from the fill guard) (DONE):](archive/M53.md) **Scope note:** D1 + D4 shipped and deployed; D2/D3 deferred to follow-up milestone (feasibility spike required — portfolio-strategy shadow fan-out not on existing single-symbol VWAP path). **D1 (shipped, live as of 2026-07-04):** Added `xmom_tp_arm_rr: z.number().positive().default(1.5)` to `packages/shared/src/schema/momentumParamsSchema.ts` (via `bot-shared-maintainer` serial first); wired the arm site only in `MomentumOrchestratorService.ts:618` to read new field; left `xmom_min_rr` as the guard-floor thread untouched (line ~857). The active version (`id=20`, `params={}`) parses to `xmom_tp_arm_rr=1.5`, identical to today — byte-for-byte no-op on ship. **D4 (deployed 2026-07-04, real DB live):** Forward-only migration `20260712000000-ArchiveRetiredVwapShadowRows.ts` executed against dev/soak database after pg_dump `backups/backup_20260704_2226.sql.gz` (1101.1M). **Post-deployment bug analysis:** Initial migration code referenced non-existent column `"id"` on `strategy_versions` table; real PK is `strategy_versions_id`. This would have caused transactional rollback on first run (failed safely, no data touched, migration not recorded as applied). Engine agent corrected the column name, hardened the test fixture to prevent real-schema divergence, re-ran full `migration.roundtrip.spec.ts` integration suite against isolated test-Postgres port 6900 (70 round-trip tests + 21 M53 unit tests = 91 total, all green); found + fixed one stale expectation (hardcoded `status='shadow'` for rows D4 now correctly archives). **Deployment result:** ids 1,2,4,15,16,17,19 now `status='archived'` with `archived_at` stamped; id=3 (active VWAP) and id=20 (xmom) untouched. Migration idempotent, reversible. **PAPER live-app smoke test (completed 2026-07-04):** Engine rebuilt + restarted post-migration; zero `level:50` (error) log lines across container lifetime; `ShadowStrategyOrchestratorService` boot log confirmed "Shadow orchestrator initialized: 0 shadows ready" (7 dead VWAP cohorts gone); active xmom version confirmed `params={}` in real DB (arm=1.5, no-op); natural rebalance cron cycles post-migration executed cleanly (opens approved through risk gate, M52 retry mechanism armed/fired normally, no errors). **D2/D3 deferred:** Multi-ratio shadow cohort + per-cohort instrumentation remain open; architect decides route 1 vs route 2 feasibility. **Tests:** 21 original adversarial + 91 additional roundtrip/integration = 112 total passing. **Review:** 1 wave (quant + security + logic + clean-code): zero blockers, zero highs; 1 MEDIUM fixed (migration comment); 2 nits fixed. **ADRs:** 0047 amended; 0045/0050/0051 referenced. **Process learning:** Mock-only migration tests can miss real schema mismatches; roundtrip-suite integration testing (full schema + real DB constraints) should be standard QA gate for any migration-bearing milestone going forward. This pattern catches divergence between test fixtures and production schemas before deployment. |
| M51 | [M51 — Unblock xmom PAPER soak (time-stop gate alignment + paper-only liquidity relax) (DONE):](plans/archive/M51-xmom-paper-gate-unblock.md) **Scope note:** D1 + D2 shipped/reviewed (2026-07-02); D3/D4 deferred. **D1 P0 blocker (shipped):** centralize momentum time-stop 2× margin in `MOMENTUM_TIME_STOP_MARGIN_MULTIPLIER` constant; both intent's `timeStopAtMs` and gate ceiling's `buildGateStrategyParams.time_stop_minutes` derive from it (prior 1× ceiling rejected every deep-book symbol on `time_stop_missing_or_invalid` at 01:07 cron — 84 of 100 attempts). **D2 P1 blocker (shipped):** paper-only per-coin liquidity relax behind default-off `PAPER_RELAX_PER_COIN_LIQUIDITY` — relaxed depth >$2,500, spread ≤0.30%, combined per-tier so relax never stricter than live (`min` with live floor for depth, `max` for spread). Unblocks thin momentum leaders (ranks 1–7) in PAPER. Never reaches LIVE (reviewer-verified test). **D3/D4 (stretch, deferred):** `depth=0` investigation (keep fail-closed), pre-gate-skip visibility. **Smoke test (2026-07-02):** 3 gate approvals + 3 thin-book rejects in first cycle — first real end-to-end xmom lifecycle in 185 prior gate attempts with 0 fills. TLM+ALLO opened/held; RE opened then flattened by pre-existing `degenerate_geometry_at_fill` guard. **Tests:** 11 paired (happy path 2 + adversarial 9): deep-book fills with 2× time-stop, thin leader fills with paper relax (flag on/off), anti-coverage flag-on+LIVE reverts to live tier1, lower-bound guard intact, no drift across intervals, floor boundaries, live constants untouched, `depth=0` still rejects, pre-gate skip surfaces. **Review:** 1 wave, zero blockers/highs, 1 MEDIUM fixed (boot warning when relax+high exposure). **ADRs:** 0048 amended (D1 time-stop 2× constant as source of truth); 0042 amended (D2 paper-only liquidity relax); 0004 §6a additive pointer (live depth unchanged). |
| M50b | [M50b — xmom cascade, top_n=3 basket, fixed 01:07 UTC rebalance (ADR 0050) (DONE):](archive/M50b.md) Paper-path increment on M50. Cascade fallback in orchestrator (full `ranked` list from pure core; `selected`→`ranked`); two-tier closes keyed on `retained`; `top_n` default 3; cron `7 1 * * *` UTC. 65 tests passing; review 1 round zero blockers/highs. H8 correlation go-live blocker added. Operator: reset id=20 params to `{}`; verify same-direction exposure cap. M50 milestone remains open. |
| M49 | [M49 — Fetch the real closing fill before finalizing RECONCILED_MISSING (LIVE H5 tech-debt fix) (DONE):](plans/archive/M49-reconciled-missing-fill-fetch.md) **Scope note:** D1 + D2 + D3 + D3.1 shipped and deployed. LIVE-mode data-integrity fix: when the bot finalizes a position as `RECONCILED_MISSING` (exchange shows it gone, but bot never recorded the close locally), M49 fetches the actual closing trades from exchange account history (`fetchMyTrades`) and records them into the ledger before finalize, so the aggregation (ADR 0012 §5) produces real `exit_price`/`realized_pnl`/`fees`. **Deliverables:** D1 `fetchMyTrades` facade + `IMyTradeSnapshot` boundary type on account-state read surface (decimal-as-string, per-trade `realizedPnl`; no `reduceOnly` phantom field, H1); D2 `recordReconciledClosingFills` aggregates per `orderId` into ONE ledger row with VWAP price + summed qty/fee + fee-currency guard (H2 BNB not summed to USDT PnL); D3 backfill-before-finalize wiring in `ReconciliationService` (case-b + case-f-closed, both LIVE finalize sites); D3.1 `RECONCILED_MISSING_UNRECOVERABLE` event on null-PnL fallback (no fetch / fetch failed). **Scope boundaries:** no finalize/aggregatePnl formula change (Option A reuses `exchange_order_id` dedup, no migration); `StuckPositionSweeper` is PAPER-only, not a LIVE H5 site; no BNB→USDT fee conversion. **PAPER inert:** reconciliation tick is a no-op; `fetchMyTrades` never called. **Tests:** 16 paired (happy path 4 + adversarial 12): backfill in both LIVE finalize sites, empty fetch graceful degradation with event fire, multi-partial aggregation without silent-drop, dedup vs prior executor REDUCE, idempotency/re-fetch, BNB fee guard, realized-PnL divergence probe, side sign, PAPER inert, determinism. **Review:** 1 wave, zero blockers/highs, 1 MEDIUM fixed. **ADRs:** 0010 amended (§1b/§1f fetch-before-finalize step + fallback); 0001 additive note (`IMyTradeSnapshot` boundary); 0012 clarifying sentence (post-M49, null PnL only when fetch unavailable). |
| M48 | [M48 — Fill-anchor geometry fix (fill-time geometry-integrity leg in `evaluateFillDrift`) (DONE):](archive/M48.md) Root cause: M47 froze SL/TP at signal time but every geometry check measures distance against reconstructed reference (`reconstructReferencePrice`), never actual fill. Live position 212 diverged 1.53% from reconstruction, collapsed `slDist` 34.5× (0.984 → 0.0285) → 85:1 R:R at fill; position 211 shipped inverted R:R 0.74. Delivered: New fill-anchored geometry-integrity leg in `evaluateFillDrift` (5 steps: side-ordering check, signed distances from fill, collapsed-stop guard, noise-floor check using `slFloor` formula, R:R ratio check) → `DEGENERATE_GEOMETRY_AT_FILL` reject via existing synthetic-FLATTEN unwind. Supporting: `geometryParams` stamped on `IOrderIntentApprovedEvent` for zero hot-path DB round-trip; `resolveSlFloorDistance` moved to `common/utils/geometryUtils.ts` (shared by both strategy + execution, Option A); `resolvedTakeProfitPrice` threaded into `IFillDriftContext`; `GEOMETRY_ANCHOR_DRIFT` ATR-unit log. Invariants preserved: no SL/TP price mutation (Option B frozen), no rebase re-introduced, live-only (backtest/shadow inert), money decimal. Tests: 26 unit + 2 integration = 28 new (≥4,000 total, 0 failures). Review: 2 waves (R1: 1 quant HIGH latent TP mismatch + 4 clean-code violations; R2 after fixes: zero blockers, zero highs). ADRs amended: 0045 (D2.9 fill-anchored leg + Amendment 1A geometryParams plumbing). |
| M47 | [M47 — R:R Geometry Fix (uncoupled SL/TP anchors + asymmetric fill-rebase) (DONE):](archive/M47.md) Root cause of −259 USDT / 29.8% win rate: Bug 1 (uncoupled SL/TP anchors in all cores), Bug 2 (asymmetric momentum fill-rebase). Delivered: Task 0 (tpRebaseEligible=false, momentum no-rebase), Task 1 (4 new versioned params: min_rr, entry_pct_floor, atr_floor_multiplier, max_tp_dist_factor; RR_TOO_LOW enum), Task 2 (momentum TP coupling to SL via rrFloor + cap), Task 3 (mean-reversion SL cap to TP), Task 4 (risk-gate R:R backstop anchored to signal reference price), Task 5a (MFE/MAE seed-timing race fix — synchronous seed in ExecutionService before first await), Task 5c (position_segment_stats view for M48 data foundation). BLOCKERs resolved: BLOCKER 1 (gate anchor parity — referencePrice field), BLOCKER 5 (rrFloor cap + degenerate skip). Deferred: Task 5b (historical backfill harness — post-deploy). Tests: 64 new tests across 6 spec files (3,983 total, 0 failures). Reviewers: 1 wave (security/logic/clean-code/quant all clean; zero blockers, zero highs, 1 MEDIUM fixed post-review). ADRs amended: 0003 (SL/TP coupling), 0045 (no-rebase Option B), 0004 (R:R backstop). Deploy: non-rolling; new shadow-status version rows v11/v21/v31 backfilled with params via JSON-merge migration; operator activates via ACTIVE_STRATEGY_VERSION_ID. Note: `min_rr=1.5` ships provisional; empirical confirmation deferred to post-deploy review on forward trades (5a seed-timing fix enables trustworthy excursion data; 5b establishes baseline only). Tech-debt M7 (MFE/MAE async seed) marked RESOLVED by Task 5a. |
| M46 | [M46 — Rate-limit ledger audit (Scenario A1: separate-ledger confirmed, host boundary split `/fapi` vs `/sapi`) (DONE):](archive/M46.md) |
| M43 | [M43 — Strategy selectivity (`catalyst_risk → skip`) + long-book RR geometry + phantom purge (DONE):](archive/M43.md) |
| M40 | [M40 — Halt-exempt closes + shadow fill regression + stuck-position sweeper (DONE):](archive/M40.md) |
| M41 | [M41 — Decisions feed outcome + zero-fill audit cashflow (DONE):](archive/M41.md) |
| M42 | [M42 — Paper stale-tick REST refresh before fill simulation (DONE):](archive/M42.md) |
| M39 | [M39 — Shadow-ledger close path + realized-PnL fidelity (W1+W2 shipped, D3 gate unblocked) (DONE):](archive/M39.md) |
| M38 | [M38 — Exit-geometry repair + fill-acceptance guard (D1+D2 deployed; D3 gated) (DONE):](archive/M38.md) |
| M37 | [M37 — Strategy-comparison infrastructure (concurrent shadow evaluation + backtest fill restoration + trade-record integrity) (DONE):](archive/M37.md) |
| M36 | [M36 — Paper soak consecutive-loss-halt relaxation (DONE):](archive/M36.md) |
| M35 | [M35 — Trade-record integrity (entry-snapshot persistence + wrong-side/sub-cost TP guards) (DONE):](archive/M35.md) |
| M0 | [M0 — Foundation & scaffolding](archive/M0.md) |
| M1 | [M1 — Exchange & market data](archive/M1.md) |
| M2 | [M2 — Persistence & data model](archive/M2.md) |
| M3 | [M3 — Strategy engine](archive/M3.md) |
| M4 | [M4 — Risk management](archive/M4.md) |
| M5 | [M5 — Execution (testnet)](archive/M5.md) |
| M5.5 | [M5.5 — Adversarial backfill (pre-M6 hardening)](archive/M5.5.md) |
| M6 | [M6 — Position management & reconciliation](archive/M6.md) |
| M7 | [M7 — Backtesting & performance](archive/M7.md) |
| M8 | [M8 — Strategy versioning & comparison](archive/M8.md) |
| M9 | [M9 — Observability, control & read API](archive/M9.md) |
| M10 | [M10 — Dashboard](archive/M10.md) |
| M11a | [M11a — Local soak hardening (PAPER mode) + Shadow-decision infrastructure](archive/M11a.md) |
| M12 | [M12 — Analysis MCP](archive/M12.md) |
| M13 | [M13 — Agentic weekly loop](archive/M13.md) |
| M14 | [M14 — CI review gate](archive/M14.md) |
| M16 | [M16 — Test-DB isolation](archive/M16.md) |
| M17 | [M17 — Automated daily DB backup (local disk, 3-deep retention)](archive/M17.md) |
| M18 | [M18 — Directional rate-limit drift alert:](archive/M18.md) |
| M19 | [M19 — Per-coin liquidity gate (stop the global liquidity halt)](archive/M19.md) |
| M21 | [M21 — Index-shock horizon alignment:](archive/M21.md) |
| M22 | [M22 — Depth-floor recalibration](archive/M22.md) |
| M23 | [M23 — Market-stress adaptive auto-resume](archive/M23.md) |
| M24 | [M24 — Live/paper open-fill wiring](archive/M24.md) |
| M25 | [M25 — Paper exploration enablement](archive/M25.md) |
| M26 | [M26 — Shadow counterfactual fill wiring](archive/M26.md) |
| M27 | [M27 — Decision data-capture completeness](archive/M27.md) |
| M28 | [M28 — Same-bar threshold recalibration + auto-resume wiring (DONE):](archive/M28.md) |
| M29 | [M29 — Paper funnel diagnosis + first-fill enablement (DONE):](archive/M29.md) |
| M30 | [M30 — Idiosyncratic-edge soak gate + idiosyncrasy observability (DONE):](archive/M30.md) |
| M31 | [M31 — Zombie positions & broken position-lifecycle (DONE):](archive/M31.md) |
| M32 | [M32 — Dashboard closed-positions history + Telegram position alerts (DONE):](archive/M32.md) |
| M34 | [M34 — Slot-reservation leak fix (normal-close CONFIRMED release + ADD multi-release + reconciliation slot-accounting invariant) (DONE):](archive/M34.md) |
| M33 | [M33 — Live exit enforcement (time-stop + paper protective simulation + entry cashflow) (DONE):](archive/M33.md) |
