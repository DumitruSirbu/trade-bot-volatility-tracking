# M32 — Dashboard closed-positions history + Telegram position alerts (DONE):

Engine-only alert widening + dashboard new UI components. No migration, no shared-package change, engine restart only. **Engine changes:** `IPositionOpenedEvent` new interface at `apps/engine/src/common/interface/IPositionOpenedEvent.ts` (4 consumers retyped; handler bodies unchanged). `IPositionClosedEvent` widened with 5 new fields: `entryPrice: MoneyValue`, `exitPrice: MoneyValue | null | undefined`, `leverage: DecimalValue`, `strategyVersionId: number`, `openedAt: Date`. New pure formatter module `positionAlertText.ts` with adaptive price precision (≥$1 → 2dp, ≥$0.01 → 4dp, else 8dp) and two total functions: `formatPositionOpenedBody` (symbol, leverage Nx, entry price, quantity) and `formatPositionClosedBody` (symbol, entry/exit prices, quantity, realized PnL labeled (net) = fillPnl − feesPaid + fundingPaid, hold duration). `RiskListeners.onPositionOpened` + `onPositionClosed` enriched with Telegram body text; `data` map built inline (never reaches wire; dashboard-only Record<string, string>, null values render 'n/a'). **Dashboard changes:** New `ClosedPositionsTable` component — 9-column cursor-paged closed-positions view (symbol, open time, close time, entry price, exit price, qty, leverage, side, realized PnL). New `PositionsPanel` segmented toggle (Open/Closed, default Open); replaces direct `<PositionsTable />` in Shell. `PositionDetail` renders CLOSED badge for terminal states. `utils.ts` adds `formatDurationMs` utility + named time constants (MILLIS_PER_SECOND, SECONDS_PER_MINUTE, SECONDS_PER_HOUR, SECONDS_PER_DAY). **Tests:** 24 new (5 positionAlertText total-function, 18 RiskListeners.m32, 1 ClosedPositionsTable zero-PnL edge). Engine: 3451 total passing. Dashboard: 235 total passing. **Review:** 1 round — zero blockers, zero highs, zero mediums across all four reviewers at close. One clean-code MUST-FIX found and resolved R1 (dead `buildClosedAlertData` helper removed; data inlined per convention). Two SHOULD-FIX resolved (named time constants in dashboard utils.ts; interface `exitPrice` null consistency check). Security, quant, logic all CLEAN in R1. **ADRs:** ADR 0044 (new) documents alert schema, position lifecycle events, adaptive price precision, realized PnL (net) labeling, data map structure. **Post-deploy:** pg_dump → engine restart (no migration) → 10-min smoke → confirm first Telegram open/close alert fires with symbol, leverage (Nx), entry/exit prices (null = liquidated), realized PnL (net), hold duration. Zero blockers, zero highs, zero mediums at close.
