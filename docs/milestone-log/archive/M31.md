# M31 — Zombie positions & broken position-lifecycle (DONE):

Bug-fix milestone. Engine-only. No schema migration, no shared-package change. Branch: `feat/m31-zombie-positions-and-broken-lifecycle`. **Defects fixed:** (D1 P0) `applyReduceFillToPosition` now promotes `PENDING_OPEN → OPEN` before any close write (ADR 0009 §6.3 two-step). Flat-row guard prevents double-close on already-zeroed rows. (D2 P0) `recordEntryTransactionOrEscalate` wrapper: non-duplicate audit persist failure emits `ORDER_AUDIT_PERSIST_FAILED_EVENT` + `ORDER_INTENT_UNKNOWN_EVENT`; arm ordering unchanged (ADR 0008 §2). (D3 P1) New `RiskStateLifecycleListener` recomputes `open_exposure` / `realized_pnl_day` / `trades_count` on `POSITION_OPENED_EVENT` + `POSITION_CLOSED_EVENT` via column-scoped `upsertAccountingForDay` (never touches halt columns). Idempotent by recompute (SELECT-then-upsert). (D5 P2) `PositionRepository.findLiveRisk()` (`qty > 0 AND non-closed`) added. `findNonTerminal()` (broad recon view) added. `OpenPositionsPortAdapter`, `PositionsController`, `AccountSnapshotWriter` repointed to `findLiveRisk()`. `ReconciliationService.loadNonClosedPositions` repointed to `findNonTerminal()`. (Task 6 boot hardening) `phase4aRebuildOpenExposure` uses residual `qty * entry_price` (not `entry_notional`) and skips `qty ≤ 0` rows. `phase4cRearmLocalMonitor` skips `qty ≤ 0` rows. The 1508.35 phantom exposure boot artifact is eliminated. (D6 not-a-defect) resume counter requires `action=open` gate evaluations; post-halt calm session lacked them. Documented as not-a-defect. **ADRs amended:** 0009 §6.3 (two-step promote generalized), 0012 §5 (lifecycle recompute), 0014 §4a (residual formula + qty=0 exclusion). **Data repair:** One-time SQL repair for three 2026-06-11 zombie rows (manual post-deploy: pg_dump → stop engine → run repair transaction → restart). **Tests:** 38 new adversarial tests across 3 new spec files. Total engine: 3406 passing. **Review:** 2 rounds. R1 found: 1 blocker (halt-clobber race in lifecycle listener), 2 highs (unhandled async rejection; findOpen callers unwired), 1 quant high (raw SQL column names). R2 found: 1 new blocker (orUpdate column names). All resolved by fix rounds. Final: zero blockers, zero highs. Mediums deferred: `persistHalt` residual clobber race (M32), ADD/partial-reduce recompute gap (M32), `decisions.position_id` gap (future). **Post-deploy sequence (mandatory before declaring live):** pg_dump → stop engine → run SQL repair (3 zombie rows, detector queries verify) → restart → 10-min smoke → first-fill watch (open tx, POSITION_OPENED_EVENT, risk_state recompute both sides). **Soak data integrity constraints:** 2026-06-11 window excluded from strategy-version promotion (regime-biased, halt-distorted); positions 1–3 excluded from idiosyncrasy entry-slippage analysis (no `open` tx rows). Zero blockers, zero highs, zero mediums at close.
