# Work Log

| Date (UTC) | Start | End | Duration | Phase / Task | Agent(s) | Outcome / Notes |
|---|---|---|---|---|---|---|
| 2026-05-28 | — | — | — | **M14 — Milestone close (scribe: docs + work-log + CLAUDE.md)** | bot-scribe | **DONE.** M14 deterministic CI gates shipped (W1–W3 complete, no LLM agents in CI per locked user scope). Repo `main` green at root level for first time. **W1:** 10 required status checks wired (install, build, typecheck, lint, format, test with Postgres service, boundary ADR-specs, SCA allowlist-filtered, lockfile single-source, exchange-dep pin+provenance). Concurrency cancels PR runs only; permissions `contents: read` least-privilege; third-party actions SHA-pinned. **W2:** Supply-chain layer (`.github/audit-allowlist.json` empty [], `.github/exchange-critical-deps.json` pinned ccxt 4.5.54/decimal.js 10.6.0/pg 8.21.0, `pnpm.auditConfig.ignoreGhsas: []` kept empty by design), pure modules `apps/engine/tests/ci/` (auditAllowlistFilter, exchangeDepPinCheck, ciPaths, runScaGate, runPinGate + 30 unit tests). **W3:** ADRs 0039–0041 (gate policy + branch protection with job-name contract, SCA + lockfile integrity + exception process with §2.2 refinement ignoreGhsas-empty binding, exchange-dep pinning + layer-2 provenance deferred). Green-up phase: eslint.config.js wired `globals` package + `^_` convention + `.agents/`/`.claude/` ignores (killed ~12,220 `no-undef`); `.prettierignore` scoped to CODE (excludes docs/prose, deliberate ADR 0039 §2.2 refinement, avoided 5,197-line low-value repad); `packages/shared` + engine typecheck scripts; prettier formatted 97 files; ~90 lint errors fixed (dead code, require-imports). **Production bug caught:** M13 agent DB-write broken — `agent_writer` role default_transaction_read_only=on, SDF SET LOCAL fires too late (must SET before any query), every agent write would FAIL in production. Invisible to mocked unit tests; only full-suite migrate-only Postgres exposed it. Fixed `AgentPgClient.ts` (explicit SET TRANSACTION READ WRITE) + paired tests. Also fixed 21 migrate-only failures (partition fixtures, stale auth_tokens refs, UUID fixtures, KeyPermission predicates, BACKTEST_ARTEFACT_ROOT path, migration count). **Test counts:** engine 2,555 pass (was 22 fail, now 0); +30 CI-gate tests; agent 261; dashboard 153; all gates green. **Review cycle:** R1 (0 blockers, 2 highs: S-H1 SCA fail-open inverted, S-H2 provenance inert) → R2 (4 reviewers, 0 blockers/highs). **Dep changes:** ccxt `^4.5.54`→`4.5.54` exact, react-router-dom `7.1.5`→`7.15.1` (cleared 4 HIGH advisories). **Runbook:** `docs/runbooks/ci-gates.md` (10 jobs, job-name contract, branch-protection payload NOT YET APPLIED, allowlist-rotation + exchange-dep procedures, emergency-revert, Postgres service, boundary-grep refinement note). **Deferred to M14.5/M15:** LLM agents in CI phase-2, coverage-threshold gate, Dependabot/Renovate, short-position funding-sign test (belt-and-suspenders). **Critical note:** branch protection MUST be APPLIED by repo owner via runbook `gh api` payload (not yet applied; M14 scope was gates + green-up + runbook, not infrastructure change). Zero blockers, zero highs at close. |
| 2026-05-27 | — | — | — | **M13 — Post-scribe live-app smoke (4 production gaps + fix waves 7–10)** | bot-engine-nestjs, bot-scribe | **DONE.** Operator ran 10-min live-app smoke test AFTER scribe close (per feedback-milestone-app-smoke memory rule) against real docker-compose stack. Uncovered 4 production gaps hidden from unit tests. **Fix wave 7** — Engine token missing `aud` claim (M13 MCP verifier requires `aud='mcp'`; every token failed BAD_SCOPE). Fix: `IJwtPayload.aud?` + `IIssueTokenInput.aud?` + `--aud` CLI flag + 5+3 paired tests. **Fix wave 8** — Bot-mcp Dockerfile + compose service (HTTP transport buildable but not deployable). Fix: multi-stage Dockerfile (non-root, HEALTHCHECK), new compose service (profile agent, internal-only port), new `MCP_HTTP_BIND_HOST` env + opt-in flag. 2 new bind-address tests. **Fix wave 9** — MCP/engine auth-key derivation mismatch (engine derives via `DerivedKeyService` info=`'auth v1'`; MCP read raw `AUTH_HS256_KEY`; different keys → signature fail). Fix: new `apps/mcp/src/transport/deriveAuthKey.ts` (10-line HKDF replica), MCP `main.ts` derives from `AUTH_HMAC_SECRET`, compose env updated. 7 new tests. **Fix wave 10** — `mcp_reader` lacked SELECT on `revoked_jti` (M9 migration excluded auth tables for least-privilege; every legit bearer hit permission-denied, mislabeled as MALFORMED). Fix: new migration `20260620000003-GrantRevokedJtiToMcpReader.ts` (reversible), HttpTransport logs unknown-error fall-throughs to stderr. Integration spec extended (SELECT works, INSERT/UPDATE/DELETE raise 42501). **Smoke verification:** All 5 containers healthy, all 4 M13 migrations + new revoked_jti grant applied, real bearer minted with `--aud mcp`, tools/list returned 6 tools, get_halt_state/get_performance/list_positions validated input contracts, agent dry-run executed end-to-end (halt-aware short-circuit fired correctly on market_stress halt). Cross-container 5-min watch: zero new errors post-fix-wave-10. **Outcome:** Stack boots clean; agent → MCP → DB proven live on real running infrastructure. Files touched: 2 source (auth) + 2 spec (W7), 1 Dockerfile + 1 compose + 2 spec (W8), 1 transport + 1 main + 1 compose + 3 spec (W9), 1 migration + 1 transport + 1 spec (W10). Zero blockers, zero highs. |
| 2026-05-27 | — | — | — | **M13 — Milestone close (scribe: docs + ADRs + work-log + CLAUDE.md)** | bot-scribe | **DONE.** W0–W6 complete: 7 waves shipped. **W0:** Agent workspace scaffold (`apps/agent/` @bot/agent), `agent_writer` DB role (SELECT on 13 whitelisted tables, 30s timeout, 5s lock timeout), `draft_strategy_version(parent_version_id, params, rationale, week_iso)` SDF (hard-codes `status='draft'`, idempotent on `parent_version_id + week_iso`), `agent_run_history` table (SDF-insert-only), ESLint boundary rule forbids @bot/engine/@bot/analysis/@bot/mcp, compose service + Dockerfile. **W1:** MCP HTTP transport (`localhost:MCP_HTTP_PORT` 127.0.0.1-only, `MCP_TRANSPORT=http` env-select, reuses M9 bearer-auth + revoked_jti), `getHaltState` 6th tool (returns `{ isHalted, haltReason, date }`), agent `McpClient` (JSON-RPC over HTTP, Zod-validated per-tool responses, 30s socket timeout standard / 10m for runBacktest), `redactForLlm` egress allowlist (ADR 0037 chokepoint: blocks auth_*/balance/equity/key/ip/order-ids, allows metrics/decision/position-symbol). **W2:** Vercel AI SDK weekly loop (`runWeeklyLoop` orchestrator), `buildPrompt` system+user composition, `ProposedDraftSchema.strict` (Zod: `{ params, rationale ≤2000ch, expectedDirection, confidence }`), `aiGateway` wrapper (default `anthropic/claude-opus-4-7`, fallback `anthropic/claude-sonnet-4-5`, cost-cap via `AI_GATEWAY_MAX_USD_PER_RUN`), `buildReport` markdown+JSON (headline, active-vs-draft table, walk-forward OOS, bootstrap CI, per-regime breakdown, ADR 0019 12-criterion evaluation, provenance). **W3:** `draftStrategyVersion` SDF caller (returns draft_id OR null on idempotent-skip), `agentRunHistory` INSERT (week_iso UNIQUE, terminal_state enum: COMPLETED/SKIPPED_HALTED/IDEMPOTENT_SKIP/FAILED), `AgentPgClient` pool (max=2, min=0, sentinel-password refusal), `runWeeklyLoop` updated (SDF first; IF null→write IDEMPOTENT_SKIP history, ELSE→write history with draft_id; separate transactions). **W4:** `runComparisonBacktests` (shared from/to window, asserts both reports match), `comparisonStats` (headline delta on expectancyPerUnitRisk, bootstrap CI pass/fail), `promotionGate` (ADR 0019 12-criterion eval, sets flag on history row; does NOT enforce), `buildReport` v2 (includes criterion table, pass/fail per criterion). **W5:** `main.ts` (acquires PID lockfile `/tmp/bot-agent.lock`, reads `AGENT_WEEK_ISO` env or `--week-iso` CLI, runs `runWeeklyLoop`, SIGTERM 45-min wallclock→SIGKILL+5s), CLI args (`--dry-run` skips writes, `--parent-version-id` override active version), ofelia cron sidecar (Sun 00:00 UTC `docker exec bot-agent node /app/dist/main.js`), runbook (`docs/runbooks/agent-weekly-loop.md` mint-bearer, env setup, manual trigger, report inspect, draft promote, password rotate). **W6a+W6b:** Adversarial QA (egress violation detection before prompt, prompt-injection isolation via tool-call sequence, LLM hallucination status-immutability at DB layer, DB-role-bypass via SDF SECURITY DEFINER, MCP transport spoofing bearer/JTI/aud, boundary compile/lint/runtime) + 4-reviewer parallel (security halt-state interaction/bearer/redaction; logic idempotency/halt-skip/promotion-gate; clean-code ≤20-line funcs/naming; quant stats consumption/no reimplementation). **ADRs 0035–0038 Accepted-and-shipped:** structural boundary (workspace deps, ESLint, tsc, runtime guard; four-gate defense-in-depth), `agent_writer` role (minimal SELECT, EXECUTE SDF only, no UPDATE), egress allowlist (single `redactForLlm` chokepoint, blocklist exhaustive, allowlist per ADR 0037), localhost HTTP MCP (127.0.0.1 bind-only, reuses M9 bearer, matches M15-eventual-cloud topology). **Test counts at close:** 259 agent (W0–W6 + review cycles), 102 analysis (M12 regression), 123 MCP (M12 regression), engine core green; DB-role integration conditional on local PG. **Review cycle:** R1 (1 BLOCKER: agent_run_history SDF must RETURN new id + idempotency ON CONFLICT order) + R2 (3 reviewers, ~10 HIGHs closed, ~18 mediums; majority mediums deferred to M14). **Fix waves:** 6 total (W0–W4 focused, W5 runbook+cron, W6 4 reviewers + 1 cleanup wave). **Deferred to M14:** SDF pre-check idempotency (cost optimization before LLM inference), pickTopSymbols hardcoded (needs engine per-symbol trade-count surface), assertSharedRunConfig (needs simulatorConfigHash + seed), 6 ADR 0019 criteria NOT_AVAILABLE (criteria 5,7,8,9,10b,11 require engine extensions). **Deferred to pre-M15:** function-size refactors (`verifyBearer`, `runUnderWallclock`, `runBoundaryGuard`), arg-count DTO refactors (composeMarkdown done), void-param suppressions (3), ESLint pragma scope (`apps/agent/src/**` override), pino redaction paths broadening. **Boundary verified at close:** `@bot/agent` imports only @bot/shared; tsc --noEmit rejects @bot/engine/@bot/analysis/@bot/mcp; ESLint blocks patterns; runtime guard scans require.cache. **Zero blockers, zero highs at close.** Majority mediums resolved; 4 items carry to M14 backlog, 5 to pre-M15 refactor pool. |
| 2026-05-27 | — | — | — | **M12 — Milestone close (scribe: docs + ADRs + work-log + CLAUDE.md) + post-close live-app smoke** | bot-scribe | **DONE.** W0–W6 complete: workspace + DB role + analysis query layer (W1) + MCP server skeleton (W3) + 5 read-only tools (W4) + adversarial QA (W5) + 5 fix waves + 4 reviewers (W6). Workspace structure: `apps/mcp/` (@bot/mcp) + `packages/analysis/` (@bot/analysis, read-only library). DB: migration `CreateMcpReaderRole.ts` provisions `mcp_reader` role (default-read-only, 30s stmt timeout, 5s lock timeout, SELECT on 13 whitelisted tables). Analysis layer: `DataSourceFactory`, 4 query functions (`getPerformance`, `compareVersions`, `listPositions`, `getDecisions`), `CursorCodec` pagination (filterHash binding), shared validation + consts. MCP server: MCP SDK stdio transport, `RuntimeGuard` (Layer C boundary check), `ToolRegistry` (write-tool registration forbidden by construction), Zod schemas + constants. Tools: `get_performance(versionId, from, to)`, `compare_versions(aVersionId, bVersionId, from, to)`, `list_positions(filters)`, `get_decisions(symbol, from, to, includeSnapshot)`, `run_backtest(versionId, from, to)` — run_backtest spawns engine CLI (absolute-path validation, env allowlist, Sema(1), 10-min SIGTERM→SIGKILL, stderr redaction of postgres URLs + Bearer tokens + IPv4/IPv6). ADRs 0033 + 0034: structural boundary enforcement (load-bearing: workspace deps forbid @bot/engine; ESLint + runtime guards defense-in-depth), DB isolation (read-only role + 30s timeout + 3-connection pool + per-tool query budgets). Test counts: @bot/analysis 94 tests, @bot/mcp 99 tests, @bot/engine M12-touched 11 tests, DB-role integration conditional on local PG; all green. Review cycle: R1 (2 blockers: compareVersions double-counting + run_backtest ISO truncation) + 5 highs, R2–R5 (4 fix waves covering SQL-injection, boundary compliance, DTO validation, redaction, clean code). Outstanding mediums deferred (MCP_ENGINE_CMD TOCTOU, GRANT CONNECT quoting, NOLOGIN rotation, missing positions_id index, windowDays Math.floor vs round, paired test + enum nits, control-flow spacing mass edit). Boundary verified: `@bot/mcp` imports only @bot/shared + @bot/analysis; tsc --noEmit rejects @bot/engine; ESLint blocks patterns; runtime guard scans require.cache. Operator runbook: `docs/runbooks/mcp-deployment.md` (sentinel-password rotation, MCP_ENGINE_CMD setup, tool invocation examples). **Post-close live-app smoke (per `feedback-milestone-app-smoke`):** Operator started MCP server for first time against real database; 13 JSON-RPC calls tested (5 tools + validation + adversarial). Smoke uncovered 3 production bugs: (1) **HIGH — symbol-regex too narrow:** `get_decisions` + `list_positions` regex `^[A-Z0-9]{1,20}$` rejected CCXT futures notation `TST/USDT:USDT`. (2) **MEDIUM — getPerformance empty-window fallback:** returned `label: "unknown@v<id>"` for valid versions when window had 0 trades (SQL didn't JOIN strategy_versions). (3) **HIGH — engine onModuleDestroy crash:** CcxtBinanceExchangeClient.close() routed through rate limiter on teardown (not in OPERATION_REQUEST_WEIGHTS), causing backtest exit code 1. Fix wave 6 landed all 3 fixes + re-smoke verified: symbol regex widened to `^[A-Z0-9]{1,20}(?:\/[A-Z0-9]{1,15}:[A-Z0-9]{1,15})?$`, getPerformance now resolves real `name@version + status` via lookupVersionOrThrow(), engine close() no longer rate-limited + errors swallowed + logged. Test counts updated: @bot/analysis 97/97 (+6), @bot/mcp 100/100 (+1), engine +2 (new `CcxtBinanceExchangeClient.onModuleDestroy.spec.ts`). `.env.example` gained "Analysis MCP (M12)" section; default `MCP_DB_PORT=5433` (matches host port mapping, prevents collision with local Postgres on 5432). Files: W0 eslint + migration; W1–W4 8 new source files (analysis + mcp); W5–W6 94 + 99 tests; fix-wave-6 3 source files touched + 3 new tests. Deferred: depth-aware backtest extension (M9+), wizard MCP tools (M13+), CLI user auth (deferred from M11). Commit-ready. |
| 2026-05-27 | — | — | — | **Pre-M15 deferred item #1 — real-PG integration tests for HMAC chain** | bot-qa-engineer | **DONE.** 12 new integration tests across BootModeHistoryRepository / BootModeChainRotationRepository / PaperStateAuditRepository. Covers atomic INSERT…RETURNING seq round-trip (pins the M11a live-smoke bug regression), multi-row chain walk + verify, cross-transaction continuity, all-zero HMAC CHECK rejection. All 12 pass against local Postgres (port 5433, db `trade_bot`); skip cleanly without RUN_PG_INTEGRATION=1. No production-code defects flagged. Files: `apps/engine/src/boot-mode-history/__tests__/BootModeHistoryRepository.integration.spec.ts` (5 blocks), `apps/engine/src/boot-mode-history/__tests__/BootModeChainRotationRepository.integration.spec.ts` (3 blocks), `apps/engine/src/paper-mode/__tests__/PaperStateAuditRepository.integration.spec.ts` (4 blocks). |
| 2026-05-27 | — | — | — | **Doc-only: Renumber M11b → M15 (cloud go-live milestone, local-first sequencing)** | bot-scribe | **DONE.** Cloud go-live milestone renumbered M11b → M15 to reflect new sequencing: M11a soak feeds into local-only M12 (Analysis MCP) / M13 (Agentic loop) / M14 (CI gate), then M15 (cloud go-live) as the final milestone. Rationale: keep all development + research work off paid infrastructure while preserving gate structure. Files modified: (1) created `M15-cloud-go-live.md` (renamed from M11b, updated title + depends-on clause + forward-pointer note), (2) `M11-go-live-hardening.md` (table row M11b→M15 + new path, added explanation paragraph), (3) `M11a-local-soak.md` (forward-pointer note in "Soak exit criteria → M11b" section anchoring historical text), (4) `00-overview.md` (milestone table reordered M12–M15, text path updated), (5) `CLAUDE.md` (Next section updated, Pre-M15 deferred items header + two M11b→M15 bullet updates). Historical references in M11a prose intentionally preserved (no mass-rename; forward-pointer sufficient). No code, no tests, no ADR edits. |
| 2026-05-27 | — | — | — | **M11a — Milestone close live-smoke fix wave (CRITICAL bugs + amendments)** | bot-engine-nestjs, bot-shared-maintainer, bot-scribe | **DONE.** Four critical bugs caught + fixed during post-R4 live-app smoke (engine PAPER boot against real Binance sub-account): (1) **HMAC chain verification fails every restart** (files: `BootModeHistoryRepository`, `BootModeChainRotationRepository`) — root cause `manager.save()` two-phase INSERT...UPDATE does NOT refresh BIGSERIAL columns from RETURNING, so write-time HMAC computed over `seq: undefined`, read-time over actual `seq: 1`, resulting in different bytes every read. Fix: switched to raw `INSERT...RETURNING` atomic capture (same pattern `PaperStateAuditRepository` already used). Both repositories now capture DB-assigned `seq` + `bootedAt`/`rotatedAt` within a single transaction, HMAC binding happens inside the same statement. (2) **`/sapi/v1/account/apiRestrictions/ipRestriction` endpoint discontinued by Binance 2021-11-17** (file: `CcxtBinanceExchangeClient.fetchKeyPermissions + toKeyPermissionSnapshot`) — root cause engine called both `/sapi/v1/account/apiRestrictions` and the dead `/sapi/v1/account/apiRestrictions/ipRestriction` endpoint; Binance returns `-1102` on every call. Only surviving `/sapi/v1/account/apiRestrictions` endpoint exposes `ipRestrict: boolean` (whitelist set or not), not the actual IP list. Fix: dropped the dead call, `ipRestriction: null` defaults in mapper, `ipAllowList` defaults to `[]`, predicate no longer requires `ipAllowList.length > 0`. Operator validates IP allow-list via Binance UI per runbook (no self-readable endpoint exists). (3) **Sub-account API key shape incompatible with allowlist defaults** (file: `toKeyPermissionSnapshot` defaults, `computeFailingClauses`, `isKeyPermissionSnapshotAcceptable` in shared) — root cause Binance `/sapi/v1/account/apiRestrictions` response OMITS fields not applicable to sub-account keys (`enableSubAccountManagement`, `enableWithdrawals`, `enableInternalTransfer`, `enableMargin`, `enableVanillaOptions`, `permitsUniversalTransfer`, `tradingAuthorityExpirationTime`). Original defensive defaults assumed "missing field = unsafe, fail closed" (designed for master-account shape). For Fallback Profile sub-account key per ADR 0032 §D8 operative spec, predicate could NEVER pass. Fix: changed defaults for sub-account-irrelevant fields from `true` to `false` (Binance structurally prevents sub-accounts from having these permissions). Removed `enableSubAccountManagement` from documented response (never a real `/sapi` field). Made `tradingAuthorityExpirationTime` optional (null accepted). **Trade-off:** lowers safety floor for LIVE if Binance master-account response shape ever changes to omit a previously-present field; documented as Pre-M11b deferred validation (verify LIVE master-account shape before go-live). (4) **Bogus CRITICAL boot alert on every successful boot** (files: `AlertTypeEnum`, `KeyPermissionAssertionService.publishBootAlert`) — root cause published `type: UNHANDLED_EXCEPTION, severity: CRITICAL` on every boot for informational message "Engine boot — exchange environment resolved"; Telegram-wired soaks would page operator on every restart. Fix: added `AlertTypeEnum.BOOT_ENGINE_STARTED`, changed publishBootAlert to use new type + `severity: INFO`. **Bonus — diagnostic stderr.** `KeyPermissionAssertionService` catch block now `process.stderr.write`s underlying ccxt error before `process.exit` (pino is async-buffered, drops diagnostics). **Why R4 reviewers missed each:** (1) & (2) & (3) — every `BootModeChainService.adversarial.spec.ts` test mocked `appendInTransaction`; no integration test exercised production transaction path against real Postgres. R4 security review #2 already flagged "real-Postgres integration test for advisory-lock + single-transaction shape" as deferred. (1) only manifests on restart. (2) & (3) — endpoint was mocked in every test; never validated against live Binance during R4; originally added against stale Binance documentation. (4) — boot-alert payload visible in test fixtures but no test asserted type/severity classification. **Engine now boots cleanly in PAPER mode end-to-end:** key-permission PASSED, boot pipeline 9/9 phases complete, all paper-mode services active, PaperExchangeNullityProbe cycling, zero restarts after fixes landed. **9–10h soak monitor running,** hourly self-checks via ScheduleWakeup, zero unhandled exceptions. Amendments to ADR 0028, ADR 0032, `CLAUDE.md` Status, `M11a-local-soak.md` (operator runbook + Pre-M11b deferred list) documented below. Files touched: 2 + 2 + 1 shared + runbook + status docs. Commit-ready. |
| 2026-05-26 | — | — | — | **M11a — Milestone close R4.2 (scribe: merge addendum + docs + work-log + CLAUDE.md)** | bot-scribe | **DONE.** Merged M11a-paper-mode-addendum.md content into M11a-local-soak.md as specified in addendum R4.2 spec. New "PAPER-mode architecture (locked decisions D1–D17)" section inserted before wave plan, preserving all anchor IDs (D1 `paper_account_state` dedicated table, D2 `IExecutionClient` order-command-only surface, D3 PaperFillSimulator determinism + idempotency ledger, D4 funding math + sign convention + force-flush, D5 MTM throttle + peak-equity denominator, D6 boot-mode HMAC chain + integrity, D7 transition matrix + append-only typed rows, D8 PAPER allowlist + Fallback Profile, D9 LIVE_GO_AHEAD_TOKEN LIVE-only, D10 closed-trade counting + missed-fill non-slot, D11 $500 starting equity, D12 reconciliation CRITICAL on drift, D13 nullity probe dual-call + capability preflight, D14 `IAccountStateSource` port + exception list, D15 FillSimulatorCore shared + causality test + numerical equivalence, D16 paper-state SoT per datum + atomicity + unrealised-PnL derived, D17 CRN per-soak root + skip-case pairing + inconclusive truth table). W1.1 wording replaced (PAPER design specifics vs DEMO rename). W1.2 updated for mode-aware allowlist (D8 Fallback Profile). W4.4 expanded: calibration day + pre-soak sanity TOST (asymmetric bands [-0.15R, +0.05R] per D10) + sample-size pre-flight (N=1000 paired variance + MDE check). "Minimum trade count" section augmented with D10 closed-trade enumeration + missed-fill non-slot. "Reduced evaluation gate" rewritten: primary criterion is "active version beats shadow v2/v3" on paired CRN CI (D17 two cohorts: trade-vs-trade + full same-event); lowFidelity downgrade (automatic inconclusive if `lowFidelity`-excluded subset empty per D10); M11b gate hardening three-branch rule (depth-aware rerun OR micro-probe OR architect waiver); joint-test acknowledgement. "TESTNET pre-M11b drill" section added (separate required gate: order-lifecycle + reconciliation + rate-limit under burst, complementary to PAPER). Definition of Done updated (PAPER + TESTNET gates, 9-table schema per D16 + D17, two cohorts + two `lowFidelity` rankings per D17, soak outcome bucket explicit). **Addendum file deleted** (unblocked by full content merge). **CLAUDE.md M11a status** rewritten: single comprehensive M11a-DONE line capturing R0–R4 complete cycle, all 9 new tables, D1–D17 locked decisions, 658 adversarial tests, 4 reviewers R4, R4 0 blockers/0 highs at close, addendum merged, D10 pre-soak sanity + TOST calibration + sample-size pre-flight gates integrated, M11b gates codified (PAPER soak + TESTNET drill both required). Pre-M11a deferred items list expanded (11 items, all retaining from prior milestones). Next line updated: M11b gates on (a) PAPER soak exit criteria met AND (b) TESTNET drill green. Files modified: `docs/plans/M11a-local-soak.md` (+748 lines, integrated D1–D17 + W1.1/W1.2/W4.4 integration + pre-soak sanity + TESTNET section), `CLAUDE.md` Status + Next (M11a status comprehensive + deferred list expanded), `docs/work-log.md` (this row). |
| 2026-05-26 | — | — | — | **M11a — Paper-mode addendum (DEMO → PAPER course correction), Wave R0 close** | architect | **DONE.** Doc-only wave. Five files modified in shared + architecture. (1) `packages/shared/src/enum/ExchangeEnvironmentEnum.ts` (6 lines) — renamed `DEMO` → `PAPER`; three values now TESTNET, PAPER, LIVE. (2) `packages/shared/src/schema/exchangeEnvironmentSchema.ts` (13 lines) — Zod schema enforces EXCHANGE_ENV presence, no defaults. (3) `packages/shared/src/util/isKeyPermissionSnapshotAcceptable.ts` (37 lines) — new MODE-AWARE allowlist predicate per D8: pure function takes `{ mode: 'paper' \| 'live' }` parameter. PAPER requires `enableFutures: false`; LIVE requires `enableFutures: true`; all other capability flags false for both; both require IP-restrict + non-empty allow-list + non-expired trading authority. (4) `docs/architecture/adr/0028-key-permission-assertion-port.md` — amended §2.3 (removed false demo-fapi claim, codifies testnet-alias reality), §2.4 rewritten as mode-aware allowlist per D8, new §2.5 records D9 (PAPER does not require LIVE_GO_AHEAD_TOKEN). (5) `docs/architecture/adr/0014-crash-recovery.md` — phase-1 amended to read via injected `IBootStateSource` dispatched on `EXCHANGE_ENV` (ExchangeBootStateSource for LIVE/TESTNET, PaperBootStateSource for PAPER); boot_mode_history HMAC-chain check runs before phase 1. (6) **NEW:** `docs/architecture/adr/0032-paper-mode-architecture.md` (~1123 lines) — complete ADR self-contained: D1–D17 reproduced verbatim with anchor IDs preserved, PaperModeModule component set, compile-time + runtime D14 invariants, nine-table DB schema, D7 transition matrix, TESTNET pre-M11b drill codified. **D8 endpoint-accessibility verification (blocker resolution):** gbt R2-M1 blocker outcome = Fallback Profile selected. Reason: Binance signed `/fapi` endpoints (`/fapi/v1/openOrders`, `/fapi/v2/positionRisk`) required by D13 nullity probe require the `enableFutures` permission; a key with `enableFutures:false` returns `-2015`. Therefore PAPER probe key runs on a **dedicated zero-balance Binance sub-account with `enableFutures:true`**, with D13's extended invariants (zero balance + zero positions + zero open orders every cycle; no transfer permissions; IP-restrict; non-null trading authority). Empirical probe-key sanity-check by operator recommended before soak starts, not required for R1 dispatch. **Ambiguities carried into R1/R2 adjudication:** (a) IBootStateSource surface shape (fetcher interface), (b) read-API mode=paper filter shape, (c) PaperBootStateSource snapshot DTO column set, (d) paper_account_state_history columns + retention policy, (e) LIVE→PAPER transition-only provider token name. Zero engine/dashboard code touched. Addendum file persists until R4.2 (after R0–R3 cycle + reviewer round complete). |
| 2026-05-25 | — | — | — | **M10 — Dashboard (React, auth, kill-switch UI)** | bot-shared-maintainer, bot-engine-nestjs, bot-dashboard-react, bot-qa-engineer, bot-review-security, bot-review-logic, bot-review-clean-code, bot-scribe | **DONE.** W0 shared-contract gap-fill: `ILoginRequest`/`ILoginResponse`, `IPnlTickEvent`/`IStreamLaggedEvent`, `IApiError`, `AuthFailureReasonEnum.BAD_SECRET`, `HaltAuditActionEnum` extended LOGIN_SUCCESS/LOGIN_FAILURE/LOGIN_THROTTLED, `IRateLimitFailure.TOO_MANY_LOGIN_ATTEMPTS`, `READ_API_PATHS` + `WS_EVENT_NAMES` constants. W0.5 engine login gate: `POST /v1/auth/login` (bootstrap-secret → HS256 JWT via existing AuthTokenService), `LoginRateLimiter` (5/10s burst + 20/600s sustained + 200/60s global), `LoginValidationFilter` mapping pipe errors to 401 MALFORMED, `LoginRequestDto` class-validator, precomputed SHA-256 hash, `ControlAuditRepository.appendLoginAudit` with 500ms timeout, migration `20260601000000-WidenControlAuditActionForLogin`. W1–W4 dashboard (Vite 6 + React 19 + TS strict + Tailwind v4 + shadcn/ui + TanStack Query v5 + socket.io-client + react-router 7 + decimal.js-light): LoginScreen (paste secret → POST /auth/login → sessionStorage); read views (positions, decisions, performance, account strip); singleton WS client + cache-merge hooks; kill-switch UI (type "HALT"/"RESUME" confirm + flatten default off) + ResumeButton + HaltBanner + HaltHistoryDrawer + PositionDetail. W5 devops: engine Dockerfile (multi-stage, non-root, INCLUDE_DEV_LOGS arg-gate pino-pretty), dashboard Dockerfile (nginx-unprivileged), nginx.conf (same-origin reverse-proxy, WS upgrade, CSP `connect-src 'self'; object-src 'none'; frame-src 'none'; frame-ancestors 'none'`, XFF overwrite via `proxy_set_header X-Forwarded-For $remote_addr`), `engine-migrator` one-shot compose service, adminer behind `--profile dev`, .env.example overhauled. ADRs 0026–0027. R1 highs: XFF spoofing (req.ip), rate-limit boundary regression, audit DB timeout, duplicate import, float money math (decimal.js-light), positionsClosed prefix invalidation, HaltBanner reason fallback, dispatchAuthExpired drops WS socket, per-page error recovery. R2 mediums: LoginRequestDto + ValidationPipe, precomputed hash, secret length cap, sourceIp in denied log, `change_me` sentinel; dashboard polish (tonForPnl→toneForPnl, dead casts, enum constant, journal-comment cleanup, magic-number consts, redundant useMemo, bindLifecycle DTO, position.closed → invalidate positionById, mutation response uses HaltStateEnum, dialog dedupe); nginx XFF overwrite, CSP tightening, adminer dev-profile, pino-pretty dev-arg gating, engine-migrator service. R3 minimal security fix (XFF spoofing validated with live-app compose). **Live-app compose smoke:** stack healthy in ~22s; XFF spoof verified — spoofed first hop never reaches audit table; LOGIN_SUCCESS/LOGIN_FAILURE rows correctly attributed to nginx-stamped IP; testnet login flow end-to-end; zero console errors. **Test count: 170 in scope (auth + control + alert + ws)** + **152 dashboard greenfield specs** (10 files); total dashboard regression = 1,967 + 170 + 152. Final: zero blockers, zero highs. Deferred M11: pino-pretty fallback when missing in engine (currently dev-arg gated); CSP not duplicated on `/index.html` location block (cosmetic); KillSwitchButton/ResumeButton dialog refactor inlined; decimal.js-light bundle size (~10 KB gzipped); stand-alone engine dev requires manual migration:run (documented in .env.example). |
| 2026-05-25 | — | — | — | **M9 — Observability, control & read API** | bot-shared-maintainer, bot-engine-nestjs, bot-qa-engineer, bot-review-security, bot-review-logic, bot-review-clean-code, bot-review-quant, bot-scribe | **DONE.** Startup schema-validation gate (PHASE 0 fail-fast), auth guard HS256 bearer + revoked_jti, HaltController + HaltRateLimiter (5/60s) + HaltStateRestoreService (Option β newer-wins), ReadApi REST + CursorCodec HMAC-guard, socket.io /live gateway (4 rooms, PerSocketQueue backpressure), TelegramAlertSink (outbound-only, 30/min + per-symbol coalesce), DailyPnlSummaryScheduler (UTC midnight), RiskListeners (market-stress + model-divergence alerts). W0: 7 enums + 14 interfaces + READ_API_VERSION. W1: startup gate, AlertModule no-op sink, control_audit migration. W2: AuthGuard + AuthCorsInterceptor + CLI auth issue/revoke. W3: HaltController + HaltService + HaltRateLimiter + HaltStateRestoreService (PHASE 3). W4: ReadApi controllers + entity→DTO mappers + CursorCodec. W5: LiveGateway + 4 rooms + handshake + sweeper + PerSocketQueue + PnL throttle + coalescer. W6: TelegramAlertSink + AlertRedactor + AlertRateLimiter + DailyPnlSummaryScheduler + RiskListeners (Option β). W6.1: M4 RiskGateService emits market-stress + model-divergence (read-only side-channel). ADRs 0020–0025. R1: 2 logic blockers + 4 highs, 2 security blockers + 6 highs, 8 clean-code violations, 1 quant blocker + 4 highs; architect adjudicated 6 contracts (halt SoT→Option β; wasAlreadyHalted; DTO nullability; CursorCodec; flatten default; CORS single source); 6 fix waves. R2: Logic 10 R1 items + 2 new highs (one HaltStateRestoreService boundary); Security all resolved; Clean-code clean; Quant 1 high (winRate nullability); 2 fix waves. R3 narrow: all clean, 2 acceptable LOWs. **Live-app smoke 10h:** caught 2 production bugs (AlertModule DI cycle → AlertSinkModule fix #18; LiveGateway.liveSockets no `.of()` → lambda fix); schema-gate manifest 3 wrong columns fixed + integration spec; 9h testnet run (EXECUTION_MODE=live, ACCOUNT $5000): 43 decisions, 14 open candidates, 0 opens, 2 market-stress halts, rejects sl_outside 7×/exposure 2×/market_stress 2×/spread 2×/global_halt 1×, zero crashes ~80MB RSS. **Test count: 1,967 passing (39 pre-existing Postgres-credential failures on M2 integration specs, unrelated to M9).** Final: zero blockers, zero highs. Deferred M11: AuthFailureReasonEnum.BAD_SIGNATURE split, BaseRepository uuid-PK widening, risk_state.updated_at, LiveGateway AppConfigService injection, HKDF cursor, revoked_jti sweep, notePragmaticTransition clamps, AUTH token TTL comment, Cache-Control halt/history. |
| 2026-05-24 | — | — | — | **M8 — Strategy versioning & comparison** | bot-engine-nestjs, bot-shared-maintainer, bot-qa-engineer, bot-review-logic, bot-review-security, bot-review-clean-code, bot-review-quant, bot-scribe | **DONE.** Head-to-head strategy comparison with walk-forward OOS splits, paired circular-block bootstrap (n=10k, 95% CI), and per-regime metrics. Direction decision (v0 baseline, v1 reversion, v2 momentum, v3 hybrid) now data-backed. W0: ExitReasonEnum.FORCE_CLOSE + M2 partition-rollover paired test. W1: OrderPolicyRouter parametrised injection (IOrderPolicyRouter DI token); v3 exercises real flow-typed routing. W2: schema delta migration (audit columns, partial uq, comparison_reports table + FK), ComparisonReportEntity + repo. W3: pure WalkForwardPlanner (rolling/expanding, JSON-stable folds), 14 tests. W4: event tape recording/replay on BacktestRunnerService; ComparisonRunnerService driver; ITapedEvent, IComparisonEventOutcome, IComparisonReport placeholders. W5a: pure stats (mulberry32, fnv1a32, politisWhite K=2, circularBlockBootstrap 10k×95%CI, tail-risk, regime breakdown), 35 tests. W5b: IBacktestTradeResult.riskBudgetSpent, engine populates from clamped stop, BootstrapStatsService paired bootstrap integrated into ComparisonRunnerService. W6: PromotionModule, PromotionGateService (12 all-of criteria; criteria 7+9 deferred W6.1 with severity:deferred → inconclusive:robustness_pending), PromotionService.promote/reactivate in SERIALIZABLE TX, promotionGateConsts, IPromotionGateOutcome, PromotionRejectedException. W7: CLI pnpm engine strategy compare/promote/reactivate; compare resolves versions, drives ComparisonRunnerService, writes full report JSON to BACKTEST_ARTEFACT_ROOT, persists slim comparison_reports row; promote rejects with structured failure table; reactivate is operator-only rollback. W8: 59 adversarial tests (WalkForwardPlanner, bootstrap math, ComparisonRunner, PromotionGate, PromotionService TX, CLI surfaces), 0 production bugs, 2 cosmetics. R-Fix 1: 1 blocker (PromotionService.requireReportPromotesVersion load artefact + validate inside TX) + 4 highs (regime counter fall-through, ADR 0018 §2.1 wording refresh, BacktestEventBuilder import, aggregateOosCells decimal). R-Fix 2: 5 mediums (artefact path allow-list + ArtefactPathOutsideRootException, Error→domain-exceptions, magic→consts+MS_PER_WEEK, criterion-11 observed carries regime, politisWhite ACF extended 2·K_N capped selectBandwidthM). R-Fix 3: 5 mediums (ConcurrentPromotionConflictException wrap PG 23505/40001 + paired test, parseFlagMap→cliArgs.ts, @InjectDataSource comment, TODO M8 W6.1 anchor, skew/kurtosis documented). Final: PromotionService.readPromotionDecision mirrors loadReportArtefact path check (defense-in-depth). Final state: 0 blockers, 0 highs, 11/12 mediums (12th CLI token deferred operator-only), 264 focused tests green, 254+ adversarial/integration green. ADRs 0016–0019 (lineage, walk-forward, paired-bootstrap, promotion-gate). Deferred M9: criteria 7+9 robustness re-runs (W6.1), partition-rollover (M2), CLI auth, eventAnchoredVwap (M11), depth-aware slippage (M9), politisWhite comment (cosmetic). |
| 2026-05-24 | — | — | — | **M7 — Backtesting & performance** | bot-engine-nestjs, bot-shared-maintainer, bot-qa-engineer, bot-review-logic, bot-review-security, bot-review-clean-code, bot-review-quant, bot-scribe | **DONE.** Per-run replay engine reproduces live strategy behavior on stored market data. W0: dropped `positions.status` legacy column and `PositionStatusEnum` (migration `20260526000000-DropPositionsStatusColumn.ts`). W1: BacktestModule foundation — `IBacktestConfig`, `IBacktestFill`, `IBacktestPosition`, `IBacktestTradeResult`, `IBacktestReport` shared interfaces; `CandleLoader`, `IndicatorStateBuilder`, `PointInTimeUniverse` services; `CausalityGuard` asserts no future-bar/same-bar data leaks; ADR-0015. W2: in-memory adapters (`BacktestPositionAdapter`, `BacktestRiskStateAdapter`, `BacktestInstrumentAdapter`), `BacktestBook`, fill simulator pipeline (`TierSlippageModel`, `LatencyModel`, `MissedFillModel`, `IntrabarStopSimulator`, `FillSimulator`), backtest constants. W3: `BacktestPnLLedger`, `BacktestEquityCurve`, `MetricsComputer` (Sharpe/Sortino √365), `FundingReplayLoader`, `BacktestExecutionSink`. W4a: `BacktestEventBuilder` (pure event builder from snapshot + context), `BacktestOrchestrator` (mirrors live strategy→gate→fill), `StrategyModule` exports `StrategyRegistry`. W4b: `BacktestRunnerService` (main per-run replay loop: data load, bar-by-bar replay, intra-bar stops, funding, force-close survivors at end-of-window), `RunBacktestCommand` CLI entry, full `BacktestModule` wiring. W5: 82 adversarial QA tests across `BacktestEventBuilder`, `BacktestOrchestrator`, `BacktestRunnerService`. R1 fix wave: 7 blocker/high fixes — `ReservationLedger` singleton leak sealed, slippage double-count removed, entry fill price corrected to next-bar open (ADR-0015 §6), daily/weekly loss limits now advance via `riskStateByDay` upsert, equity curve includes end-of-window force-closed positions, ticks loaded once per bar, OI deltas + funding rate derived from loaded data. R2 fix wave: 5 high/medium fixes — `updateRiskStateAfterClose` read-modify-writes halt state, `CausalityGuard.assertNoLookAhead` wired into replay loop, time-stop fill timestamp corrected, funding boundary changed to inclusive, missing BTC bars flagged as `lowFidelity`. Final test count: 1571 passing (82 new backtest tests; 61 pre-existing DB integration tests fail without Postgres — unchanged from pre-M7). Known approximations (documented in M7 outcome): `lowFidelity` always true until depth-aware slippage (M8), entry notional used for funding (not mark-to-market), `force_close` exits use `exitReason: 'time_stop'` (pending enum update M8), cross-symbol intrabar metrics set to 0, OI/funding now persisted-data-derived. Review: 2 rounds, zero blockers/highs at close. Zero production bugs by QA or reviews. Deferred to M8: OrderPolicyRouter injection, `eventAnchoredVwap` reconstruction, `force_close` exit reason enum, depth-aware slippage. |
| 2026-05-24 | — | — | — | **M6 — Position management & reconciliation** | bot-shared-maintainer, bot-engine-nestjs, bot-qa-engineer, bot-review-logic, bot-review-security, bot-review-clean-code, bot-review-quant, bot-scribe | **DONE.** 8 implementation waves (W0–W8) + smoke-test wave (W9). Positions survive restart with strategy/risk context; reconcile against exchange via explicit drift policy; stay protected/tracked after leaving universe; account for funding. W0: shared-contract enums + events + schema migration (6 new columns). W1: state machine `PENDING_OPEN|OPEN|CLOSING|CLOSED|RECONCILING|MANUAL_ADOPTED_UNMANAGED`, `PositionService.transition` single mutation API, 46 tests. W1.5: entry starts at `PENDING_OPEN` (surgical fix to state-machine coherence), 4 paired tests. W2: `SubscriptionRetainer` + held-symbol invariant, 4 tests. W3: `LocalProtectiveMonitor` `@OnEvent('price.update')` eval loop, 4 tests. W4a+W4b: `IExchangeClient` port + `ReconciliationService` skeleton + 6 drift cases (a-adopt, b-unresolved, c-qty-log, d-split, e-protective-drift, f-intent-ttl) + mutation primitives (`reconcileClose`, `recordExposureDrift`, `adjustQty`), 11 tests. W5: funding ingestion + `transactions.cashflow` + realized/unrealized PnL, 6 tests. W6: `PositionInstrumentor` lifetime stats (MAE/MFE/time-to-reversion/stop-gap/protective-type) batched flush, 6 tests. W7: `account_snapshots` writer (scheduler + reconciliation-forced + boot), 4 tests. W8: `EngineBootstrapService` 10-phase recovery (drift sweep + expose rebuild + cooldown rebuild + monitor re-arm + retainer rebuild + subscriptions + snapshot + jobs + orchestrator open), 11 tests. Adversarial QA: 78 tests (reconciliation race, restart-during-closing, foreign-position-policy-flip, cooldown-retention-release, funding-boundary, instrumentor-flush-race, boot-phase-ordering, RECONCILING_HOLD-reject, TTL-backstop, account-snapshot-drift-alert). Review cycle: R1 (5 blockers + 8 highs logic, 2 highs quant, 6 mediums security, 8 must-fixes clean), R1.1/1.2/1.3a/b/c (18-item fix waves + architect ADR revisions), R2 (2 minor must-fixes clean, 1 medium security, 4 highs + 10 mediums logic), R2.1 (7-item fix wave), R3 (1 blocker + 1 high + mediums logic), R3.1 (4-item fix wave), R4 (1 high + 1 medium logic), R4.1 (2-item fix wave), R5 (logic clean, all reviewers clean). Total: 851 focused tests (position + execution + risk); zero production bugs by QA or reviews. Deferred to M7 W0: reservation linkage (contingent), re-export shims (cosmetic), empty-object casts (tech debt). Pre-go-live blockers flagged: exposure-clamp-no-alert (2.2.3), adoption-always-slot-A (2.2.5), no-post-boot-guard (2.2.7). M2 deferred (partition-rollover for fresh Postgres) carried forward. Ready for M7. |
| 2026-05-23 | — | — | — | **M5.5 — Adversarial backfill (pre-M6 hardening)** | bot-qa-engineer (fresh per milestone), bot-architect, bot-review-logic (resumed per milestone) | **DONE.** Retrofit adversarial / failure-mode test coverage onto M1–M5 (sequence: M2→M3→M4→M5→M1) under new `dev-qa-cycle.md` rules before M6 starts. Process initiative, not feature milestone; zero new product surface. Strict 3-round cap per milestone (no escape valves). All 5 milestones pass: M2 (2 rounds, 2 real bugs + 2 test-fidelity corrections, 60 adversarial tests), M3 (1 round, 0 findings, 30 tests), M4 (1 round, 0 real bugs + 1 fidelity reclassification, 26 tests), M5 (1 round, 0 findings, 20 tests), M1 (1 round, 0 findings + 3 producer-guard carryover from M2 audit, 36 tests). Aggregate: 172 adversarial tests added, unit-test count 942→1054, 2 production bugs fixed (M2 non-finite Decimal, M2 concurrent race), 3 test-fidelity corrections, 2 ADR clarifications. M2 deferred: partition-rollover under fresh Postgres (named pre-M6 task). Round distribution: M2 2, M3 1, M4 1, M5 1, M1 1 — all ≤3. Zero blockers, zero highs at every close. Adversarial suites remain in regression; no tests deleted. Process validation: new dev-qa-cycle rules reduced cycle count (M5 took 5 rounds old process, M2 took 2 new process) + defect leakage (real bugs vs fidelity corrections isolated early). Ready for M6 start. |
| 2026-05-23 | — | — | — | **M5 — Execution (testnet)** | bot-engine-nestjs, bot-shared-maintainer, bot-qa-engineer, bot-review-* | **DONE.** ExecutionModule with idempotent open/add/reduce/close. Policies: marketable-limit-IOC (slippage-capped entries), post-only-maker (re-entry confirmation), reduce-market (fast exits). FillAccumulator tracks partial fills, recomputes SL/TP and notional from actual qty. LocalProtectiveMonitor arms SL/TP before await, disarms on execution, fallback to local enforcement when exchange rejects. Idempotency via deterministic clientOrderId keying per ADR 0006; timeout recovery queries by clientOrderId before retry. Contracts: flowType + midAtTrigger on IOrderIntent; reject taxonomy (RETRIABLE/TERMINAL/UNKNOWN per ADR 0006 §4); per-policy awaitPolicyTimeout branches; arm-before-await + disarm-before-await invariants; weighted-avg ADD entry recompute; all retry hops HaltFlagService-gated. ccxt 4.5.54 testnet with `disableFuturesSandboxWarning: true`. EXECUTION_MODE config: dry-run vs live. 898 unit tests (50 suites pass; 3 Postgres-integration env-blocked). 2 reversible migrations: AddPositionProtectiveOrderTypeDefault (uq_transactions_client_order_id), RelaxTransactionPositionIdNullable (zero-fill audit rows, down is destructive). 5 review rounds: blockers 8→1→1→0→0, highs 6→5→2→1→0. Deferred to M6/M7/M8: LocalProtectiveMonitor eval loop (M6), re-arm OPEN-status positions on save-exception (M6), position reconciliation on boot (M6), entry-side fees subtracted from realized PnL (M7/M8), BNB→USDT fee normalization (M7/M8). M2 follow-up: book_snapshots.mid_at_trigger column deferred to M9 (M5 uses StrategyService.reconstructReferencePrice; live/backtest parity via in-memory). Testnet smoke runbook documented in outcome: 10-step procedure (Postgres start, migrations, env config, engine start, signal await, transaction/position queries, idempotency verify, protective-order verify, close position). |
| 2026-05-23 | — | — | — | **M4 — Risk management** | bot-shared-maintainer, bot-engine-nestjs, bot-qa-engineer, bot-review-* | **DONE.** Central bypass-proof risk gate (RiskService synchronous evaluate, all actions routed through, never around). Position sizing via ATR-based formula (positionNotional = riskPerTrade / (atr_14 × params.atr_stop_multiplier)), max leverage 3×, decimal.js. 3-slot model: Slot A+B (idiosyncratic-only, max 2 concurrent), Slot C (BTC-correlated, max 1); if all 3 filled reject max_positions_reached. BTC-correlated single-candidate: collect all signals in same bar window, approve highest signal_score for Slot C, reject others btc_correlated_not_best_candidate. Daily & weekly loss limits (UTC midnight daily, rolling-7-day aggregate): breach halts entries. In-flight exposure reservation ledger: reserve at approval, release on reject/TTL. Funding-rate filter (suppress 50% if unfavorable direction + abs(rate) >= threshold; full suppress if annualized > 30%); funding-as-skip flow rules (rising-OI+not-extreme → skip, short-squeeze → skip, falling-OI-on-spike → allow). Spread filter (tier-based max: tier1=0.15%, tier2=0.30%, tier3=0.50%) + universe-floor check. SL-inside-liquidation distance (maintenance-margin aware, SL must trigger before liquidation). Mandatory time-stop for v1 (reject if missing or exceeds params.time_stop_minutes). Cooldown post-loss per symbol. Market-stress halt (BTC/ETH shocks, breadth, OI shock, funding-extreme, spread-widening, depth-collapse → reject mean-reversion entries). Consecutive-loss halt (default 2 per day → halt new entries UTC remainder). Overtrading caps: per-symbol, per-bar universe, same-direction portfolio. OI required gate. Tier-3 validation gate. Isolated margin default. Model-divergence kill-switch. Gate covers ALL actions (open/add/reduce/close; exits/flatten always pass through, never reject). New enums in shared: RejectReasonEnum, RiskOutcomeEnum, OrderIntentActionEnum, CorrelationModeEnum. New interfaces in risk/interface/: IRiskLimits, IRiskGateContext, IApprovedRiskDecision. Migration 20260523010000-AddPositionCorrelationMode.ts adds correlation_mode column + ENUM. ADR 0004 locked. 700 passing engine tests (152 new round-1, ~30 round-2 additions): sizing, slots (3-cap, BTC-1-cap, idio checks), daily/weekly loss, reservations (no leaks, TTL, concurrent), funding (suppression, thresholds, 50%-cut, annualized, skip flows), spread (NaN-fail-closed, tier bounds), SL-inside-liquidation (maintenance-margin math), time-stop, cooldown, market-stress, consecutive-loss, per-bar-cap (bar-index off-by-one round-2 fix), per-symbol, portfolio caps, OI gate, tier-3 gate, isolated-margin config, all-actions-routed (no bypass), decision logging. Review Round 1: liquidation-distance missing maintenance-margin (fixed SL math, subtract MM from SL adverse %), funding 50%-cut ignored threshold (apply threshold first), per-bar cap used never-written counter (track via createdAtMs same-bar reservations), stress+consecutive-loss halts unrecorded (added risk_state columns), incomplete funding-as-skip rules (added 3 separate cases). Clean-code: float math on book depth (→ decimal), magic strings/numbers (extracted consts), env limits ignored (config injection), unbounded weekly window (rolling-7-day aggregate), ETH stress horizon mismatch (parity threshold). Quant: correlation_mode missing column (added migration + real Enum), `as`-cast pile-up (refactored), function-size/spacing (fixed), NaN fail-closed (guards), redundant I/O (consolidated). Round 2: per-bar off-by-one (createdAtMs===nowMs excluded by <nowMs, fixed with buffer), stop-side missing in clampStopInsideLiquidation (short SL > entry, long SL < entry), spread filter NaN not independent (fixed), 3 magic numbers extracted to riskConsts (WEEKLY_LOSS_WINDOW_DAYS, AGG_TRADE_BUY_FLOW_BALANCE, MARKET_BREADTH_NEUTRAL_PCT), IRiskLimits/IRiskGateContext moved to risk/interface/. Build/lint/tsc clean. Zero blockers/highs end-state. Carry-overs: ADD-not-vetted (M4 scope, gate vets open, ADD safety M5), trailing-bar correlated idle flush (M6), weekly SUM query (M5+), StrategyService 12-dep refactor (M5), env defaults for restricted-live (config policy, code safe). |
| 2026-05-22 | — | — | — | **M3 — Strategy engine** | bot-shared-maintainer, bot-engine-nestjs, bot-qa-engineer, bot-review-* | **DONE.** 4 pure versioned strategies (v0 baseline no-trade, v1 exhaustion-confirmed mean-reversion fade with regime gate, v2 momentum follow with ranging-regime skip, v3 hybrid flow router); StrategyRegistry + active-version selection by config (`ACTIVE_STRATEGY_VERSION_ID` env, default v0); StrategyService orchestrator stamps flow_type + signal_score + event_id on snapshot once, reads/freezes open position, runs active strategy, writes dry-run decisions (no risk/execution emit). IStrategy pure interface (evaluate: input→ISignal), IStrategyInput (event + snapshot + openPosition + params + barMs), ISignal + IProposedExit (strategy proposes, M4/M6 enforce). Shared enums: FlowTypeEnum 5-class taxonomy (forced_exhaustion|trend_initiation|market_beta|catalyst_risk|low_quality_noise), SignalActionEnum, SkipReasonEnum, SignalTypeEnum, StopTypeEnum, CorrelationModeEnum. Shared utils: classifyFlowType (pure, idiosyncratic-trap guard), computeSignalScore (flow-aware, funding-aware). Shared schema: strategyParamsSchema typed + strict, marketSnapshotSchema strict. MarketData stamps eventId=symbol:entryCandleOpenTime. 202 tests all green (v0–v3, determinism, exhaustion, regime, v3 routing, trap, score, stops, orchestrator stamp, registry). Review Round 1: logic blocker (trap guard dead code, %B confirm always-true, div-by-zero, funding bug), quant highs (idio flow-blind, score unit), clean-code highs. Round 1 fixes: trap explicit CATALYST_RISK, thresholds params-driven, decimal.js, flow-aware signal_score (momentum-raise-idio reversion-lower), tier-band refine guard, v1 strict %B re-entry, CorrelationModeEnum. Round 2: residual clean-code (spacing, redundant throw→refine, const name, schema tabs), all fixed. Zero blockers/highs end-state. Carry-overs: prior-candle-extreme omitted (no wire field), market_stress skip deferred to M4 (risk-layer), signal_score funding side-agnostic (M4 directional carry), orchestrator placeholders (active_positions_count=0, position_slot=A until M4 slot assignment). |
| 2026-05-22 | — | — | — | **M2 — Persistence & data model** | bot-shared-maintainer, bot-engine-nestjs, bot-qa-engineer, bot-review-* | **DONE.** 13 entities: market-data owns instruments/candles/tick_aggregates/open_interest/funding_rates/book_snapshots/universe_membership; thin strategy/position/risk module shells own their entities from day one (no later churn). Reversible migrations (CreateSchema + SeedStrategyVersions on Baseline anchor); NUMERIC↔decimal.js ValueTransformer rejecting JS number; tick_aggregates native daily RANGE partitions (90-day retention, 1s OHLC); @OnEvent persistence listeners (6 new emit points: CANDLE_CLOSED 1m+5m, TICK_AGGREGATE 1s, OPEN_INTEREST_SAMPLED, FUNDING_RATE_OBSERVED at 8h settlement, INSTRUMENT_REFRESHED, UNIVERSE_SYMBOL_TIER_CHANGED); point-in-time universe_membership (gap-free tier timeline, partial unique enforcing 1 open row/symbol); strategy_versions v0–v3 seeded (v0 active no-trade, v1/v2/v3 draft); decisions.market_snapshot Zod-validated at write (warn-not-throw); DecimalValue alias separating non-money decimals. 353 Jest tests all pass; migration round-trip (run/revert/re-run) verified on real Postgres; live testnet persistence verified (1m+5m candles, 1s tick bucketing reconstructs intra-candle spike, instruments with correct tick/step, atomic tier-change universe_membership). ADR 0002 surfaced 3 decisions: (a) entity ownership re-read as infra (PersistenceModule owns cross-cutting transformer+timeline; domain modules own tables per code-conventions); (b) coin_tier stored as CoinTierEnum string varchar not SMALLINT (overrides brief); (c) zod added to packages/shared. Review Round 1: logic found 2 blockers (tick_aggregates no 1s bucketing, funding_time poll vs 8h settlement) + highs (non-atomic tier-change, dupe symbolEntered, non-tradable flip missing, index drop missing, MoneyValue misuse, empty where:{}, listener deps); clean-code blocker (magic constants). Round 2: all round-1 verified + clean-code residual typing (leverage/tickSize/stepSize/rate, MS_PER_HOUR). Post-review: caught wrong ccxt tickSize mapping (limits.price.min vs precision.price); fixed, verified BTC=0.1/ETH=0.01. Zero blockers/highs end-state. Carry-overs: OI/funding/book_snapshots accumulate 5-min+around-decision (verify >5min run); insert-failure on missing partition logged not alarmed (M9); book_snapshots fully wired M3+; decisions/positions/transactions have schema+repos+Zod hook, no live writer until M3–M6. **[M2 Follow-up, targeted M9]** Engine does not fail-fast on missing/unmigrated schema (observed in testnet: Postgres logs showed "relation 'candles'/'open_interest' does not exist" while engine booted normally); @OnEvent persistence silently swallows errors by design (ADR 0002 §4, prevents destabilizing trading loop) but creates silent data gaps. Recommend: startup guard to verify all migrations applied or expected tables exist, FAIL-FAST-and-alarm if missing, instead of degrading silently (aligns with ADR 0002 §6 risk of undetected data loss). Not an M2 blocker (persistence schema and event flow verified working end-to-end). |
| 2026-05-22 | — | — | — | **M1 — Exchange & market data** | bot-engine-nestjs, bot-shared-maintainer, bot-qa-engineer, bot-review-* | **DONE.** ExchangeModule (ccxt Binance USDT-M futures testnet, sole Binance chokepoint, read-only `IExchangeClient`; no order path). MarketDataModule (universe loader tier assignment, `!ticker@arr` WebSocket, 5-min candle aggregation, closed-bars-only indicators: session+20bar VWAP multi-anchor, σ-band, ATR14, ADX14±DI, RSI14, Bollinger %B, volume ratio; enriched signals including BTC reference, idiosyncrasy, regime, OI polling, funding, aggressor imbalance, book depth, breadth, universe age, market-stress inputs, empirical calibration; shared `evaluateTrigger` pure deterministic detector unit-tested). 251 Jest tests, all gates green. Three review rounds: Round 1 raised 2 logic blockers (bar-close tick-dependent → quiet symbols stalled; enrichment Date.now()) + highs (session VWAP never reset, partial WS seed, double snapshot, idio window, ccxt errors leaked creds); Round 2 verified fixes but found REGRESSION (bar-close half-applied → actives never evaluated) + residual header-form key leak; Round 3 unified `handleClosedBars` close authority + exactly-once watermark + sanitizer fix + batched tick pass → zero blockers/highs. Carry-overs documented: registry cleanup, market-stress placeholder, volume approximation, calibration convention, token redaction, array-by-reference. See `docs/plans/M1-exchange-market-data.md` Outcome and `docs/architecture/adr/0001-exchange-and-market-data.md`. |
| 2026-05-22 | — | — | — | **M0 — Foundation & scaffolding** | bot-engine-nestjs, bot-shared-maintainer, bot-qa-engineer, bot-review-* | **DONE.** pnpm monorepo + Docker Compose (Postgres + Adminer) + NestJS 11 app (typed AppConfig, HaltFlagService kill-switch, decimal.js money helpers + redaction scrubber, event bus, scheduler, `/health` liveness). 93 Jest tests, all gates green (build, lint, tsc, test). Two review rounds complete: Round 1 fixed money-helper dead guard, config NaN gap, EXCHANGE_TESTNET typo, single-level redaction, BaseRepository `as never`, barrel nits; Round 2 fixed magic-string const + dead-export + test nit. Carry-overs: float risk-limit config→parseMoney (M3), dual rounding context HALF_EVEN (M3), HaltReasonEnum (M6), ValidationPipe+throttler (M9), Node 22 CI (M14), .env.example DB_PASSWORD annotation (CI/prod). TypeScript version gap (packages/shared ^5.4.5 vs root 5.9.3) noted, non-blocking. See `docs/plans/M0-foundation.md` Outcome section and this log. |
