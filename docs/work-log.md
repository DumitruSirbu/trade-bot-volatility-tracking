# Work Log

| Date (UTC) | Start | End | Duration | Phase / Task | Agent(s) | Outcome / Notes |
|---|---|---|---|---|---|---|
| 2026-05-24 | — | — | — | **M7 — Backtesting & performance** | bot-engine-nestjs, bot-shared-maintainer, bot-qa-engineer, bot-review-logic, bot-review-security, bot-review-clean-code, bot-review-quant, bot-scribe | **DONE.** Per-run replay engine reproduces live strategy behavior on stored market data. W0: dropped `positions.status` legacy column and `PositionStatusEnum` (migration `20260526000000-DropPositionsStatusColumn.ts`). W1: BacktestModule foundation — `IBacktestConfig`, `IBacktestFill`, `IBacktestPosition`, `IBacktestTradeResult`, `IBacktestReport` shared interfaces; `CandleLoader`, `IndicatorStateBuilder`, `PointInTimeUniverse` services; `CausalityGuard` asserts no future-bar/same-bar data leaks; ADR-0015. W2: in-memory adapters (`BacktestPositionAdapter`, `BacktestRiskStateAdapter`, `BacktestInstrumentAdapter`), `BacktestBook`, fill simulator pipeline (`TierSlippageModel`, `LatencyModel`, `MissedFillModel`, `IntrabarStopSimulator`, `FillSimulator`), backtest constants. W3: `BacktestPnLLedger`, `BacktestEquityCurve`, `MetricsComputer` (Sharpe/Sortino √365), `FundingReplayLoader`, `BacktestExecutionSink`. W4a: `BacktestEventBuilder` (pure event builder from snapshot + context), `BacktestOrchestrator` (mirrors live strategy→gate→fill), `StrategyModule` exports `StrategyRegistry`. W4b: `BacktestRunnerService` (main per-run replay loop: data load, bar-by-bar replay, intra-bar stops, funding, force-close survivors at end-of-window), `RunBacktestCommand` CLI entry, full `BacktestModule` wiring. W5: 82 adversarial QA tests across `BacktestEventBuilder`, `BacktestOrchestrator`, `BacktestRunnerService`. R1 fix wave: 7 blocker/high fixes — `ReservationLedger` singleton leak sealed, slippage double-count removed, entry fill price corrected to next-bar open (ADR-0015 §6), daily/weekly loss limits now advance via `riskStateByDay` upsert, equity curve includes end-of-window force-closed positions, ticks loaded once per bar, OI deltas + funding rate derived from loaded data. R2 fix wave: 5 high/medium fixes — `updateRiskStateAfterClose` read-modify-writes halt state, `CausalityGuard.assertNoLookAhead` wired into replay loop, time-stop fill timestamp corrected, funding boundary changed to inclusive, missing BTC bars flagged as `lowFidelity`. Final test count: 1571 passing (82 new backtest tests; 61 pre-existing DB integration tests fail without Postgres — unchanged from pre-M7). Known approximations (documented in M7 outcome): `lowFidelity` always true until depth-aware slippage (M8), entry notional used for funding (not mark-to-market), `force_close` exits use `exitReason: 'time_stop'` (pending enum update M8), cross-symbol intrabar metrics set to 0, OI/funding now persisted-data-derived. Review: 2 rounds, zero blockers/highs at close. Zero production bugs by QA or reviews. Deferred to M8: OrderPolicyRouter injection, `eventAnchoredVwap` reconstruction, `force_close` exit reason enum, depth-aware slippage. |
| 2026-05-24 | — | — | — | **M6 — Position management & reconciliation** | bot-shared-maintainer, bot-engine-nestjs, bot-qa-engineer, bot-review-logic, bot-review-security, bot-review-clean-code, bot-review-quant, bot-scribe | **DONE.** 8 implementation waves (W0–W8) + smoke-test wave (W9). Positions survive restart with strategy/risk context; reconcile against exchange via explicit drift policy; stay protected/tracked after leaving universe; account for funding. W0: shared-contract enums + events + schema migration (6 new columns). W1: state machine `PENDING_OPEN|OPEN|CLOSING|CLOSED|RECONCILING|MANUAL_ADOPTED_UNMANAGED`, `PositionService.transition` single mutation API, 46 tests. W1.5: entry starts at `PENDING_OPEN` (surgical fix to state-machine coherence), 4 paired tests. W2: `SubscriptionRetainer` + held-symbol invariant, 4 tests. W3: `LocalProtectiveMonitor` `@OnEvent('price.update')` eval loop, 4 tests. W4a+W4b: `IExchangeClient` port + `ReconciliationService` skeleton + 6 drift cases (a-adopt, b-unresolved, c-qty-log, d-split, e-protective-drift, f-intent-ttl) + mutation primitives (`reconcileClose`, `recordExposureDrift`, `adjustQty`), 11 tests. W5: funding ingestion + `transactions.cashflow` + realized/unrealized PnL, 6 tests. W6: `PositionInstrumentor` lifetime stats (MAE/MFE/time-to-reversion/stop-gap/protective-type) batched flush, 6 tests. W7: `account_snapshots` writer (scheduler + reconciliation-forced + boot), 4 tests. W8: `EngineBootstrapService` 10-phase recovery (drift sweep + expose rebuild + cooldown rebuild + monitor re-arm + retainer rebuild + subscriptions + snapshot + jobs + orchestrator open), 11 tests. Adversarial QA: 78 tests (reconciliation race, restart-during-closing, foreign-position-policy-flip, cooldown-retention-release, funding-boundary, instrumentor-flush-race, boot-phase-ordering, RECONCILING_HOLD-reject, TTL-backstop, account-snapshot-drift-alert). Review cycle: R1 (5 blockers + 8 highs logic, 2 highs quant, 6 mediums security, 8 must-fixes clean), R1.1/1.2/1.3a/b/c (18-item fix waves + architect ADR revisions), R2 (2 minor must-fixes clean, 1 medium security, 4 highs + 10 mediums logic), R2.1 (7-item fix wave), R3 (1 blocker + 1 high + mediums logic), R3.1 (4-item fix wave), R4 (1 high + 1 medium logic), R4.1 (2-item fix wave), R5 (logic clean, all reviewers clean). Total: 851 focused tests (position + execution + risk); zero production bugs by QA or reviews. Deferred to M7 W0: reservation linkage (contingent), re-export shims (cosmetic), empty-object casts (tech debt). Pre-go-live blockers flagged: exposure-clamp-no-alert (2.2.3), adoption-always-slot-A (2.2.5), no-post-boot-guard (2.2.7). M2 deferred (partition-rollover for fresh Postgres) carried forward. Ready for M7. |
| 2026-05-23 | — | — | — | **M5.5 — Adversarial backfill (pre-M6 hardening)** | bot-qa-engineer (fresh per milestone), bot-architect, bot-review-logic (resumed per milestone) | **DONE.** Retrofit adversarial / failure-mode test coverage onto M1–M5 (sequence: M2→M3→M4→M5→M1) under new `dev-qa-cycle.md` rules before M6 starts. Process initiative, not feature milestone; zero new product surface. Strict 3-round cap per milestone (no escape valves). All 5 milestones pass: M2 (2 rounds, 2 real bugs + 2 test-fidelity corrections, 60 adversarial tests), M3 (1 round, 0 findings, 30 tests), M4 (1 round, 0 real bugs + 1 fidelity reclassification, 26 tests), M5 (1 round, 0 findings, 20 tests), M1 (1 round, 0 findings + 3 producer-guard carryover from M2 audit, 36 tests). Aggregate: 172 adversarial tests added, unit-test count 942→1054, 2 production bugs fixed (M2 non-finite Decimal, M2 concurrent race), 3 test-fidelity corrections, 2 ADR clarifications. M2 deferred: partition-rollover under fresh Postgres (named pre-M6 task). Round distribution: M2 2, M3 1, M4 1, M5 1, M1 1 — all ≤3. Zero blockers, zero highs at every close. Adversarial suites remain in regression; no tests deleted. Process validation: new dev-qa-cycle rules reduced cycle count (M5 took 5 rounds old process, M2 took 2 new process) + defect leakage (real bugs vs fidelity corrections isolated early). Ready for M6 start. |
| 2026-05-23 | — | — | — | **M5 — Execution (testnet)** | bot-engine-nestjs, bot-shared-maintainer, bot-qa-engineer, bot-review-* | **DONE.** ExecutionModule with idempotent open/add/reduce/close. Policies: marketable-limit-IOC (slippage-capped entries), post-only-maker (re-entry confirmation), reduce-market (fast exits). FillAccumulator tracks partial fills, recomputes SL/TP and notional from actual qty. LocalProtectiveMonitor arms SL/TP before await, disarms on execution, fallback to local enforcement when exchange rejects. Idempotency via deterministic clientOrderId keying per ADR 0006; timeout recovery queries by clientOrderId before retry. Contracts: flowType + midAtTrigger on IOrderIntent; reject taxonomy (RETRIABLE/TERMINAL/UNKNOWN per ADR 0006 §4); per-policy awaitPolicyTimeout branches; arm-before-await + disarm-before-await invariants; weighted-avg ADD entry recompute; all retry hops HaltFlagService-gated. ccxt 4.5.54 testnet with `disableFuturesSandboxWarning: true`. EXECUTION_MODE config: dry-run vs live. 898 unit tests (50 suites pass; 3 Postgres-integration env-blocked). 2 reversible migrations: AddPositionProtectiveOrderTypeDefault (uq_transactions_client_order_id), RelaxTransactionPositionIdNullable (zero-fill audit rows, down is destructive). 5 review rounds: blockers 8→1→1→0→0, highs 6→5→2→1→0. Deferred to M6/M7/M8: LocalProtectiveMonitor eval loop (M6), re-arm OPEN-status positions on save-exception (M6), position reconciliation on boot (M6), entry-side fees subtracted from realized PnL (M7/M8), BNB→USDT fee normalization (M7/M8). M2 follow-up: book_snapshots.mid_at_trigger column deferred to M9 (M5 uses StrategyService.reconstructReferencePrice; live/backtest parity via in-memory). Testnet smoke runbook documented in outcome: 10-step procedure (Postgres start, migrations, env config, engine start, signal await, transaction/position queries, idempotency verify, protective-order verify, close position). |
| 2026-05-23 | — | — | — | **M4 — Risk management** | bot-shared-maintainer, bot-engine-nestjs, bot-qa-engineer, bot-review-* | **DONE.** Central bypass-proof risk gate (RiskService synchronous evaluate, all actions routed through, never around). Position sizing via ATR-based formula (positionNotional = riskPerTrade / (atr_14 × params.atr_stop_multiplier)), max leverage 3×, decimal.js. 3-slot model: Slot A+B (idiosyncratic-only, max 2 concurrent), Slot C (BTC-correlated, max 1); if all 3 filled reject max_positions_reached. BTC-correlated single-candidate: collect all signals in same bar window, approve highest signal_score for Slot C, reject others btc_correlated_not_best_candidate. Daily & weekly loss limits (UTC midnight daily, rolling-7-day aggregate): breach halts entries. In-flight exposure reservation ledger: reserve at approval, release on reject/TTL. Funding-rate filter (suppress 50% if unfavorable direction + abs(rate) >= threshold; full suppress if annualized > 30%); funding-as-skip flow rules (rising-OI+not-extreme → skip, short-squeeze → skip, falling-OI-on-spike → allow). Spread filter (tier-based max: tier1=0.15%, tier2=0.30%, tier3=0.50%) + universe-floor check. SL-inside-liquidation distance (maintenance-margin aware, SL must trigger before liquidation). Mandatory time-stop for v1 (reject if missing or exceeds params.time_stop_minutes). Cooldown post-loss per symbol. Market-stress halt (BTC/ETH shocks, breadth, OI shock, funding-extreme, spread-widening, depth-collapse → reject mean-reversion entries). Consecutive-loss halt (default 2 per day → halt new entries UTC remainder). Overtrading caps: per-symbol, per-bar universe, same-direction portfolio. OI required gate. Tier-3 validation gate. Isolated margin default. Model-divergence kill-switch. Gate covers ALL actions (open/add/reduce/close; exits/flatten always pass through, never reject). New enums in shared: RejectReasonEnum, RiskOutcomeEnum, OrderIntentActionEnum, CorrelationModeEnum. New interfaces in risk/interface/: IRiskLimits, IRiskGateContext, IApprovedRiskDecision. Migration 20260523010000-AddPositionCorrelationMode.ts adds correlation_mode column + ENUM. ADR 0004 locked. 700 passing engine tests (152 new round-1, ~30 round-2 additions): sizing, slots (3-cap, BTC-1-cap, idio checks), daily/weekly loss, reservations (no leaks, TTL, concurrent), funding (suppression, thresholds, 50%-cut, annualized, skip flows), spread (NaN-fail-closed, tier bounds), SL-inside-liquidation (maintenance-margin math), time-stop, cooldown, market-stress, consecutive-loss, per-bar-cap (bar-index off-by-one round-2 fix), per-symbol, portfolio caps, OI gate, tier-3 gate, isolated-margin config, all-actions-routed (no bypass), decision logging. Review Round 1: liquidation-distance missing maintenance-margin (fixed SL math, subtract MM from SL adverse %), funding 50%-cut ignored threshold (apply threshold first), per-bar cap used never-written counter (track via createdAtMs same-bar reservations), stress+consecutive-loss halts unrecorded (added risk_state columns), incomplete funding-as-skip rules (added 3 separate cases). Clean-code: float math on book depth (→ decimal), magic strings/numbers (extracted consts), env limits ignored (config injection), unbounded weekly window (rolling-7-day aggregate), ETH stress horizon mismatch (parity threshold). Quant: correlation_mode missing column (added migration + real Enum), `as`-cast pile-up (refactored), function-size/spacing (fixed), NaN fail-closed (guards), redundant I/O (consolidated). Round 2: per-bar off-by-one (createdAtMs===nowMs excluded by <nowMs, fixed with buffer), stop-side missing in clampStopInsideLiquidation (short SL > entry, long SL < entry), spread filter NaN not independent (fixed), 3 magic numbers extracted to riskConsts (WEEKLY_LOSS_WINDOW_DAYS, AGG_TRADE_BUY_FLOW_BALANCE, MARKET_BREADTH_NEUTRAL_PCT), IRiskLimits/IRiskGateContext moved to risk/interface/. Build/lint/tsc clean. Zero blockers/highs end-state. Carry-overs: ADD-not-vetted (M4 scope, gate vets open, ADD safety M5), trailing-bar correlated idle flush (M6), weekly SUM query (M5+), StrategyService 12-dep refactor (M5), env defaults for restricted-live (config policy, code safe). |
| 2026-05-22 | — | — | — | **M3 — Strategy engine** | bot-shared-maintainer, bot-engine-nestjs, bot-qa-engineer, bot-review-* | **DONE.** 4 pure versioned strategies (v0 baseline no-trade, v1 exhaustion-confirmed mean-reversion fade with regime gate, v2 momentum follow with ranging-regime skip, v3 hybrid flow router); StrategyRegistry + active-version selection by config (`ACTIVE_STRATEGY_VERSION_ID` env, default v0); StrategyService orchestrator stamps flow_type + signal_score + event_id on snapshot once, reads/freezes open position, runs active strategy, writes dry-run decisions (no risk/execution emit). IStrategy pure interface (evaluate: input→ISignal), IStrategyInput (event + snapshot + openPosition + params + barMs), ISignal + IProposedExit (strategy proposes, M4/M6 enforce). Shared enums: FlowTypeEnum 5-class taxonomy (forced_exhaustion|trend_initiation|market_beta|catalyst_risk|low_quality_noise), SignalActionEnum, SkipReasonEnum, SignalTypeEnum, StopTypeEnum, CorrelationModeEnum. Shared utils: classifyFlowType (pure, idiosyncratic-trap guard), computeSignalScore (flow-aware, funding-aware). Shared schema: strategyParamsSchema typed + strict, marketSnapshotSchema strict. MarketData stamps eventId=symbol:entryCandleOpenTime. 202 tests all green (v0–v3, determinism, exhaustion, regime, v3 routing, trap, score, stops, orchestrator stamp, registry). Review Round 1: logic blocker (trap guard dead code, %B confirm always-true, div-by-zero, funding bug), quant highs (idio flow-blind, score unit), clean-code highs. Round 1 fixes: trap explicit CATALYST_RISK, thresholds params-driven, decimal.js, flow-aware signal_score (momentum-raise-idio reversion-lower), tier-band refine guard, v1 strict %B re-entry, CorrelationModeEnum. Round 2: residual clean-code (spacing, redundant throw→refine, const name, schema tabs), all fixed. Zero blockers/highs end-state. Carry-overs: prior-candle-extreme omitted (no wire field), market_stress skip deferred to M4 (risk-layer), signal_score funding side-agnostic (M4 directional carry), orchestrator placeholders (active_positions_count=0, position_slot=A until M4 slot assignment). |
| 2026-05-22 | — | — | — | **M2 — Persistence & data model** | bot-shared-maintainer, bot-engine-nestjs, bot-qa-engineer, bot-review-* | **DONE.** 13 entities: market-data owns instruments/candles/tick_aggregates/open_interest/funding_rates/book_snapshots/universe_membership; thin strategy/position/risk module shells own their entities from day one (no later churn). Reversible migrations (CreateSchema + SeedStrategyVersions on Baseline anchor); NUMERIC↔decimal.js ValueTransformer rejecting JS number; tick_aggregates native daily RANGE partitions (90-day retention, 1s OHLC); @OnEvent persistence listeners (6 new emit points: CANDLE_CLOSED 1m+5m, TICK_AGGREGATE 1s, OPEN_INTEREST_SAMPLED, FUNDING_RATE_OBSERVED at 8h settlement, INSTRUMENT_REFRESHED, UNIVERSE_SYMBOL_TIER_CHANGED); point-in-time universe_membership (gap-free tier timeline, partial unique enforcing 1 open row/symbol); strategy_versions v0–v3 seeded (v0 active no-trade, v1/v2/v3 draft); decisions.market_snapshot Zod-validated at write (warn-not-throw); DecimalValue alias separating non-money decimals. 353 Jest tests all pass; migration round-trip (run/revert/re-run) verified on real Postgres; live testnet persistence verified (1m+5m candles, 1s tick bucketing reconstructs intra-candle spike, instruments with correct tick/step, atomic tier-change universe_membership). ADR 0002 surfaced 3 decisions: (a) entity ownership re-read as infra (PersistenceModule owns cross-cutting transformer+timeline; domain modules own tables per code-conventions); (b) coin_tier stored as CoinTierEnum string varchar not SMALLINT (overrides brief); (c) zod added to packages/shared. Review Round 1: logic found 2 blockers (tick_aggregates no 1s bucketing, funding_time poll vs 8h settlement) + highs (non-atomic tier-change, dupe symbolEntered, non-tradable flip missing, index drop missing, MoneyValue misuse, empty where:{}, listener deps); clean-code blocker (magic constants). Round 2: all round-1 verified + clean-code residual typing (leverage/tickSize/stepSize/rate, MS_PER_HOUR). Post-review: caught wrong ccxt tickSize mapping (limits.price.min vs precision.price); fixed, verified BTC=0.1/ETH=0.01. Zero blockers/highs end-state. Carry-overs: OI/funding/book_snapshots accumulate 5-min+around-decision (verify >5min run); insert-failure on missing partition logged not alarmed (M9); book_snapshots fully wired M3+; decisions/positions/transactions have schema+repos+Zod hook, no live writer until M3–M6. **[M2 Follow-up, targeted M9]** Engine does not fail-fast on missing/unmigrated schema (observed in testnet: Postgres logs showed "relation 'candles'/'open_interest' does not exist" while engine booted normally); @OnEvent persistence silently swallows errors by design (ADR 0002 §4, prevents destabilizing trading loop) but creates silent data gaps. Recommend: startup guard to verify all migrations applied or expected tables exist, FAIL-FAST-and-alarm if missing, instead of degrading silently (aligns with ADR 0002 §6 risk of undetected data loss). Not an M2 blocker (persistence schema and event flow verified working end-to-end). |
| 2026-05-22 | — | — | — | **M1 — Exchange & market data** | bot-engine-nestjs, bot-shared-maintainer, bot-qa-engineer, bot-review-* | **DONE.** ExchangeModule (ccxt Binance USDT-M futures testnet, sole Binance chokepoint, read-only `IExchangeClient`; no order path). MarketDataModule (universe loader tier assignment, `!ticker@arr` WebSocket, 5-min candle aggregation, closed-bars-only indicators: session+20bar VWAP multi-anchor, σ-band, ATR14, ADX14±DI, RSI14, Bollinger %B, volume ratio; enriched signals including BTC reference, idiosyncrasy, regime, OI polling, funding, aggressor imbalance, book depth, breadth, universe age, market-stress inputs, empirical calibration; shared `evaluateTrigger` pure deterministic detector unit-tested). 251 Jest tests, all gates green. Three review rounds: Round 1 raised 2 logic blockers (bar-close tick-dependent → quiet symbols stalled; enrichment Date.now()) + highs (session VWAP never reset, partial WS seed, double snapshot, idio window, ccxt errors leaked creds); Round 2 verified fixes but found REGRESSION (bar-close half-applied → actives never evaluated) + residual header-form key leak; Round 3 unified `handleClosedBars` close authority + exactly-once watermark + sanitizer fix + batched tick pass → zero blockers/highs. Carry-overs documented: registry cleanup, market-stress placeholder, volume approximation, calibration convention, token redaction, array-by-reference. See `docs/plans/M1-exchange-market-data.md` Outcome and `docs/architecture/adr/0001-exchange-and-market-data.md`. |
| 2026-05-22 | — | — | — | **M0 — Foundation & scaffolding** | bot-engine-nestjs, bot-shared-maintainer, bot-qa-engineer, bot-review-* | **DONE.** pnpm monorepo + Docker Compose (Postgres + Adminer) + NestJS 11 app (typed AppConfig, HaltFlagService kill-switch, decimal.js money helpers + redaction scrubber, event bus, scheduler, `/health` liveness). 93 Jest tests, all gates green (build, lint, tsc, test). Two review rounds complete: Round 1 fixed money-helper dead guard, config NaN gap, EXCHANGE_TESTNET typo, single-level redaction, BaseRepository `as never`, barrel nits; Round 2 fixed magic-string const + dead-export + test nit. Carry-overs: float risk-limit config→parseMoney (M3), dual rounding context HALF_EVEN (M3), HaltReasonEnum (M6), ValidationPipe+throttler (M9), Node 22 CI (M14), .env.example DB_PASSWORD annotation (CI/prod). TypeScript version gap (packages/shared ^5.4.5 vs root 5.9.3) noted, non-blocking. See `docs/plans/M0-foundation.md` Outcome section and this log. |
