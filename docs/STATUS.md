# Project status

Living snapshot (~15 lines). **Single writer = scribe** at milestone close. Do not duplicate this block in `CLAUDE.md`.

| Field | Value |
|-------|-------|
| **ACTIVE** | **M44** — Shadow-fill fidelity B5 verification gate (soak accumulation, no fix required). ≥30 clean v3 fills from ≥2026-06-21; currently ≈10 fills, need ≈9–10 more soak days. |
| **Last DONE** | **M49** — Reconciled-missing fill fetch (H5 resolved). Branch: `feat/m49-reconciled-missing-fill-fetch`. Delivered: D1 — read-only `fetchMyTrades` facade + `IMyTradeSnapshot` boundary type (decimal-as-string, no `reduceOnly` field); ported to `IAccountStateSource` (LIVE + PAPER). D2 — `recordReconciledClosingFills` groups fetched trades by `orderId`, aggregates into one row per order (VWAP price, summed qty/fee), dedup on `exchange_order_id`, fee-currency guard (BNB→zero-for-PnL + WARN), realized-PnL divergence probe (> 1% or > $0.10 → WARN). D3 — `backfillClosingFillsFromExchange` helper called before `finalizeRealizedPnl` in both LIVE sites (case-b `handleDbOpenNotOnExchange`, case-f `transitionOutOfReconciling`). D3.1 — structured `RECONCILED_MISSING_UNRECOVERABLE` event emitted on graceful degradation (empty fetch or fetch failure). Key design: per-`orderId` aggregation avoids unique-constraint collision; `realizedPnl != 0` discriminator selects closing fills (not phantom `reduceOnly`); fill window `[openedAt, nowMs]`; PENDING_OPEN rows skip backfill (null PnL correct); close never blocked by fetch failure (ADR 0010 §6). Tests: 76 passing across 7 suites (3 new M49 specs). Review: 2 waves; R1 found 1 clean-code blocking + 1 cross-reviewer HIGH + 2 clean-code HIGHs + 3 MEDIUMs; R2 found 1 logic MEDIUM + 2 clean-code MEDIUMs; all resolved. Zero blockers, zero highs after fixes. ADRs: 0010 §1b/§1f amended, 0001 additive note, 0012 §5 clarifying sentence. No DB migration. Shared contract `IReconciledMissingUnrecoverableEvent` added. |
| **Deploy** | **LIVE as of 2026-06-25T20:54:44Z** — `ACTIVE_STRATEGY_VERSION_ID`: 3 → 16 (M47-coupled geometry). DB partition: `positions.strategy_version_id = 16` for all post-M47 trades. `position_segment_stats` view populated going forward. M44 soak continues (B5 gate ≥30 fills). **2026-06-30 hotfix applied:** Alert pipeline observability logs + admin test-alert endpoint (no behavioral change; root cause of Jun 25 missing notifications unconfirmed pending log inspection). |
| **Next queue** | **M44 B5 soak closure** (≥30 fills, re-measure from 2026-06-21; unblocks v3-promotion evaluation). Then: **M15 cloud go-live** (gated on B5 + v3-edge evaluation); **D6 branch-protection ops** (operator). |
