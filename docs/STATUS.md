# Project status

Living snapshot (~15 lines). **Single writer = scribe** at milestone close. Do not duplicate this block in `CLAUDE.md`.

| Field | Value |
|-------|-------|
| **ACTIVE** | **M44** — Shadow-fill fidelity B5 verification gate (soak accumulation, no fix required). ≥30 clean v3 fills from ≥2026-06-21; currently ≈10 fills, need ≈9–10 more soak days. **M52** — xmom force_close retry. D1–D3 shipped/reviewed/deployed (2026-07-03); soak regression found (M52a): cooldown + consecutive-loss checks stacked, blocking all retries. **Fix on `fix/m52-xmom-retry-cooldown-conflict` branch:** cooldown exemption for retry entries, exit-reason filtering on consecutive-loss (force_close excluded); all review waves passed, ready to merge. **D4 pending** — soak force_close + retry event accumulation with working mechanism. ADR 0051 §M52a final. |
| **Last DONE** | **M53** — xmom take-profit arm headroom (D1+D4 fully deployed 2026-07-04). **D1 deployed:** `xmom_tp_arm_rr: z.number().positive().default(1.5)` shipped to `momentumParamsSchema.ts`; active version (`id=20`, `params={}`) parses to 1.5 — byte-for-byte no-op. **D4 deployed:** Migration `20260712000000-ArchiveRetiredVwapShadowRows.ts` executed against real dev/soak database after pg_dump `backup_20260704_2226.sql.gz` (1101.1M); 7 dead `volatility-vwap` shadow rows (ids 1,2,4,15,16,17,19) now archived with `status='archived'`, `archived_at` stamped; migration applied idempotent + reversible. **D2/D3 deferred** (feasibility spike required: architect decides route 1 cascade fan-out vs route 2 offline replay). |
| **Deploy** | **LIVE as of 2026-06-25T20:54:44Z** — `ACTIVE_STRATEGY_VERSION_ID`: 3 → 16 (M47-coupled geometry). DB partition: `positions.strategy_version_id = 16` for all post-M47 trades. `position_segment_stats` view populated going forward. **2026-06-30 hotfix applied:** Alert pipeline observability logs + admin test-alert endpoint (no behavioral change; root cause of Jun 25 missing notifications unconfirmed pending log inspection). **2026-07-02:** M50c manual-rebalance trigger endpoint (REST + CLI), `positions.trigger_source` column for schedule/manual attribution, analysis exclusion filters (all paper-scoped, admin-only). **2026-07-02 M51 smoke test DONE:** engine rebuilt with D1+D2 fixes, PAPER soak unblocked (3 gate approvals first cycle, TLM + ALLO opened and held, RE opened then flattened by pre-existing `degenerate_geometry_at_fill` guard). **2026-07-03 M52 D1–D3 deployed:** engine rebuilt with force_close retry mechanism, flag active in paper soak, pg_dump `backup_20260703_1838.sql.gz` taken pre-migration. **2026-07-04 M53 D1+D4 deployed:** D1 arm/guard decoupling (no-op default) shipped to active version; D4 migration executed against real dev/soak DB after pg_dump `backup_20260704_2226.sql.gz` (1101.1M), 7 VWAP rows archived; engine restarted, PAPER live-app smoke test confirmed (zero `level:50` errors, shadow orchestrator ready, xmom arm verified at 1.5, rebalance cron cycles cleanly). Concurrent background soak: M44 B5 gate (≥30 v3 fills from 2026-06-21; ≈10 accumulated). |
| **Next queue** | **M44 B5 soak closure** (≥30 fills, re-measure; unblocks v3-promotion evaluation). **M52 D4 soak closure** (force_close + retry events accumulate, go/no-go evaluation). **M15 cloud go-live** (gated on B5 + v3-edge evaluation). |
