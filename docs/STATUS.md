# Project status

Living snapshot (~15 lines). **Single writer = scribe** at milestone close. Do not duplicate this block in `CLAUDE.md`.

| Field | Value |
|-------|-------|
| **ACTIVE** | **M44** — Shadow-fill fidelity B5 verification gate (soak accumulation, no fix required). ≥30 clean v3 fills from ≥2026-06-21; currently ≈10 fills, need ≈9–10 more soak days. |
| **Last DONE** | **M48** — Fill-anchor geometry fix. Branch: `feat/m48-geometry-fill-anchor-fix`. Delivered: One new fill-time geometry-integrity leg in `evaluateFillDrift` (side-ordering check, noise-floor guard using `slFloor` formula, R:R ratio check) anchored to the actual fill price. Rejects degenerate fills via `FILL_ACCEPTANCE_REJECTED`/`DEGENERATE_GEOMETRY_AT_FILL` through existing synthetic-FLATTEN unwind. Stamped `geometryParams` on `IOrderIntentApprovedEvent` for zero hot-path DB round-trip. Moved `resolveSlFloorDistance` to `common/utils/geometryUtils.ts` (shared by both strategy and execution layers, Option A). Threaded `resolvedTakeProfitPrice` into `IFillDriftContext`. ATR-unit `GEOMETRY_ANCHOR_DRIFT` logging at the call site. Option B (no-rebase) preserved; live-only; backtest/shadow inert. Tests: 26 unit + 2 integration = 28 new. Review: 2 waves; wave 1 found 1 quant HIGH (latent rebase TP mismatch) + 4 clean-code violations, all resolved in wave 2. Zero blockers, zero highs after fixes. ADRs: 0045 amended (D2.9 fill-anchored leg + Amendment 1A). |
| **Deploy** | **LIVE as of 2026-06-25T20:54:44Z** — `ACTIVE_STRATEGY_VERSION_ID`: 3 → 16 (M47-coupled geometry). DB partition: `positions.strategy_version_id = 16` for all post-M47 trades. `position_segment_stats` view populated going forward. M44 soak continues (B5 gate ≥30 fills). **2026-06-30 hotfix applied:** Alert pipeline observability logs + admin test-alert endpoint (no behavioral change; root cause of Jun 25 missing notifications unconfirmed pending log inspection). |
| **Next queue** | **M44 B5 soak closure** (≥30 fills, re-measure from 2026-06-21; unblocks v3-promotion evaluation). Then: **M15 cloud go-live** (gated on B5 + v3-edge evaluation); **D6 branch-protection ops** (operator). |
