# Project status

Living snapshot (~15 lines). **Single writer = scribe** at milestone close. Do not duplicate this block in `CLAUDE.md`.

| Field | Value |
|-------|-------|
| **ACTIVE** | **M44** — Shadow-fill fidelity B5 verification gate (soak accumulation, no fix required). ≥30 clean v3 fills from ≥2026-06-21; currently ≈10 fills, need ≈9–10 more soak days. **M52** — xmom force_close retry. D1–D3 shipped/reviewed/deployed (2026-07-03); soak regression found (M52a): cooldown + consecutive-loss checks stacked, blocking all retries. **Fix on `fix/m52-xmom-retry-cooldown-conflict` branch:** cooldown exemption for retry entries, exit-reason filtering on consecutive-loss (force_close excluded); all review waves passed, ready to merge. **D4 pending** — soak force_close + retry event accumulation with working mechanism. ADR 0051 §M52a final. |
| **Last DONE** | **M54** — xmom entry geometry: arm against the EXPECTED fill price (D1–D5 shipped 2026-07-09, correctness/fee-bleed fix, NOT an edge fix). New params `xmom_expected_fill_enabled` (default false) + `xmom_max_depth_fraction` (default null) on `momentumParamsSchema.ts`, byte-identical no-op on the active version (`id=20`, `params={}`). D2 arms SL/TP off expected-fill price `F_exp = P0 × (1+halfSpread/100)` + fail-closed pre-send thin-book skip when enabled; `referencePrice` stays `P0` (M48/M52 breakers untouched). D3 offline replay extended, registered EXP-025 (decision-grade read PENDING paper soak). 29 new tests, 4359 total passing. 1 review round, zero blockers, 1 HIGH + 3 MEDIUMs fixed; 2 items carried to tech-debt (H9 hardcoded DB credential in `xmom_tp_ratio_replay.mjs`; L35 `buildMomentumOpenIntent` size). ADR 0047 amended (§7); 0045/0051/0004 §6a referenced. Ships default-off — no migration, xmom stays PAPER-only. **M53** — xmom take-profit arm headroom (D1+D4 fully deployed 2026-07-04). **D1 deployed:** `xmom_tp_arm_rr: z.number().positive().default(1.5)` shipped to `momentumParamsSchema.ts`; active version (`id=20`, `params={}`) parses to 1.5 — byte-for-byte no-op. **D4 deployed:** Migration `20260712000000-ArchiveRetiredVwapShadowRows.ts` executed against real dev/soak database after pg_dump `backup_20260704_2226.sql.gz` (1101.1M); 7 dead `volatility-vwap` shadow rows (ids 1,2,4,15,16,17,19) now archived with `status='archived'`, `archived_at` stamped; migration applied idempotent + reversible. **D2/D3 deferred** (feasibility spike required: architect decides route 1 cascade fan-out vs route 2 offline replay). **EXP-020 (2026-07-06):** short-leg xmom NO-GO. Net +0.31%/pd (t=0.20) vs long +1.52%/pd; edge decayed out-of-sample (L-S gross +6.00 → +1.83); both legs negative in newest window; all short numbers are upper-bound (no borrow/funding/fast-mover charged). Keep long-only, re-run monthly. |
| **Deploy** | **LIVE as of 2026-06-25T20:54:44Z** — `ACTIVE_STRATEGY_VERSION_ID`: 3 → 16 (M47-coupled geometry). DB partition: `positions.strategy_version_id = 16` for all post-M47 trades. `position_segment_stats` view populated going forward. **2026-06-30 hotfix applied:** Alert pipeline observability logs + admin test-alert endpoint (no behavioral change; root cause of Jun 25 missing notifications unconfirmed pending log inspection). **2026-07-02:** M50c manual-rebalance trigger endpoint (REST + CLI), `positions.trigger_source` column for schedule/manual attribution, analysis exclusion filters (all paper-scoped, admin-only). **2026-07-02 M51 smoke test DONE:** engine rebuilt with D1+D2 fixes, PAPER soak unblocked (3 gate approvals first cycle, TLM + ALLO opened and held, RE opened then flattened by pre-existing `degenerate_geometry_at_fill` guard). **2026-07-03 M52 D1–D3 deployed:** engine rebuilt with force_close retry mechanism, flag active in paper soak, pg_dump `backup_20260703_1838.sql.gz` taken pre-migration. **2026-07-04 M53 D1+D4 deployed:** D1 arm/guard decoupling (no-op default) shipped to active version; D4 migration executed against real dev/soak DB after pg_dump `backup_20260704_2226.sql.gz` (1101.1M), 7 VWAP rows archived; engine restarted, PAPER live-app smoke test confirmed (zero `level:50` errors, shadow orchestrator ready, xmom arm verified at 1.5, rebalance cron cycles cleanly). **2026-07-09 M54 shipped default-off:** no DB migration required (JSONB strategy params); no engine deploy triggered by this close-out — code lands on `feat/m54-xmom-entry-geometry-expected-fill` pending merge, both new params inert at their no-op defaults on the active version. Concurrent background soak: M44 B5 gate (≥30 v3 fills from 2026-06-21; ≈10 accumulated). |
| **Next queue** | **M44 B5 soak closure** (≥30 fills, re-measure; unblocks v3-promotion evaluation). **M52 D4 soak closure** (force_close + retry events accumulate, go/no-go evaluation). **M54 enablement decision** (soak-gated, not yet unlocked — turn on `xmom_expected_fill_enabled` on the paper soak and read force_close-rate/fee-churn per plan §10 once M52 D4/M44 B5 free up soak attention). **M15 cloud go-live** (gated on B5 + v3-edge evaluation). |
