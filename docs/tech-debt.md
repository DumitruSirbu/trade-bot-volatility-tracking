# Tech Debt

Deferred items from milestone close logs. Prioritised: **HIGH** = blocks M15 go-live or has production impact, **MEDIUM** = feature or correctness gap, **LOW** = cosmetic/refactor/nit.

Items resolved or no longer applicable should be removed. New items added at the bottom of their priority group with the originating milestone.

## HIGH — Go-live blockers

| Item | File / Location | Origin | Notes |
|------|----------------|--------|-------|
| Verify LIVE master-account `/sapi/v1/account/apiRestrictions` response shape — sub-account and master-account shapes diverge | `KeyPermissionAssertionService` | M11a | Predicate could silently pass unsafe key if master-account omits a field; must re-validate Binance docs vs real endpoint |
| `risk_state.updated_at` true newer-wins upsert (W2.4) | `RiskStateRepository` | M11 | Pre-soak blocker; depends on TESTNET validation |
| Rate-limit drift `header-used ≈ 1` anomaly — investigate whether public market-data endpoint weights tally on different Binance IP-weight ledger than local bucket assumes | `apps/engine/src/exchange/` | M17 incident | Cross-ref `docs/plans/M18-rate-limit-drift-directional-alert.md` |
| `AuthFailureReasonEnum.BAD_AUDIENCE` — verifier currently uses `BAD_SCOPE` for audience mismatch | `apps/engine/src/auth/` | M13 | Live-smoke gap |
| Engine auth CLI token TTL cap is 900s — weekly agent runs need automated minting or long-lived-token issuance | `apps/agent/` | M13 | Agent will fail after 15 min without token refresh |
| Branch protection payload NOT YET APPLIED by repo owner | GitHub repo settings | M14 | Must apply via `docs/runbooks/ci-gates.md` before any live merge |

## MEDIUM — Feature / correctness gaps

| Item | File / Location | Origin | Notes |
|------|----------------|--------|-------|
| M15 soak evaluator wave (CRN tape, TOST calibration, sample-size pre-flight, lowFidelity rankings) | `docs/plans/M11a-local-soak.md` | M11a | Gate before M15 cloud go-live |
| **[M21] Backtest BTC index-shock candle vs rolling-window divergence.** `BacktestRunnerService.resolveBtcMovePct` computes a candle close-to-open return; live `btc_5m_move_pct` is a rolling 5-minute tape-window move at bar-close. Before M21 the BTC leg was inert — this gap is now active. Effect: historical replays will show fewer BTC-driven index halts than live production (candle body understates intra-bar rolling moves). Acceptable fail-direction (fewer false halts on replay, not missed real ones), but replays cannot faithfully reproduce BTC index-halt frequency. Fix: reconstruct a rolling-window BTC tape move in the backtest runner from per-bar tick/trade data. Defer until BTC index-halt frequency is empirically established from the 14-day post-deploy soak (ADR 0004 §6c). | `apps/engine/src/backtest/service/BacktestRunnerService.ts:902-916` | M21 | Measurement fidelity gap, not a correctness bug; fail-direction is safe |
| **[MEDIUM] Breadth-stress calibration (merged row).** | `STRESS_BREADTH_DISTANCE_PCT` / `MARKET_STRESS_RESUME_CLEAR_TICKS` / `MARKET_STRESS_RESUME_BREADTH_DISTANCE` in `apps/engine/src/risk/const/riskConsts.ts` | M19 → M22 → M23 | **Sub-bullets:** (1) **Validate `MARKET_STRESS_RESUME_CLEAR_TICKS = 3`** from the full per-bar consecutive-clean-bars-until-next-breach distribution with a held-out sub-period (BLOCKER 2 residual from M23; N=3 ships as a tunable starting point, not a validated calibration). (2) **Validate the resume hysteresis inner band (distance=30)** against per-bar breadth autocorrelation after a 30–60 day soak (the resume band reuses the old M19 engage value, so it must be confirmed, not assumed). (3) **Signal-dependent N for BTC/ETH shock, OI, funding legs** (deferred until real events exist; those legs stay full-day locked in M23). M22 recalibrated entry depth from M19's conservative round numbers; M23 added resume mechanism with hysteresis + per-day cap. This merged entry closes when soak evidence validates all three sub-bullets. |
| **[MEDIUM] Volume-only tier ranking — depth-blind.** | `CoinTierEnum` assignment in `ExchangeModule` | M22 | Coins are tiered by 24h volume, which ranked MAGMA ($529 depth) and H ($5,380 depth) as TIER_1 despite thin books on 2026-06-04. M22's `$10k` TIER_1 depth floor defends the symptom (filters the impostors at the depth guard) but the ranking itself is wrong. Requires a depth-aware or blended ranking to be a real fix. Not a go-live blocker; defer until after the 14-day slippage telemetry informs what tier re-ranking improves. (Added M22) |
| **[MEDIUM] Entry-vs-exit depth gap — `book_depth_10bps_usdt` does not proxy exit liquidity.** | `RiskGateService`, `PositionSizer` | M22 | A coin can pass the per-coin entry depth guard and then gap on a stop-loss exit when its book thins. The only reactive backstop is `MODEL_DIVERGENCE_SLIPPAGE_RATIO = 2` (kill switch fires after a bad fill, not before). `MODEL_DIVERGENCE_SLIPPAGE_RATIO` is not a preventive guard. The 14-day M22 post-deploy fills are gathering the evidence to size an exit-liquidity-aware position sizing fix. This entry closes when those fills validate a tighter per-coin floor or an exit-liquidity model is built. (Added M22) |
| **[MEDIUM] M24 — Post-resume directional cooldown (deferred, escalate conditionally to HIGH).** | RiskGateService, StrategyModule | M23 | After a SURGE-triggered auto-resume, suppress fresh fade/reversion entries on the surged cohort for M bars (squeeze risk); after a COLLAPSE-triggered resume, suppress fresh long entries similarly. Distinct from the halt mechanism — a post-resume guard. M23 ships the core auto-resume mechanism first (HIGH 3 defer). This is MEDIUM but **escalate to HIGH** the moment the paper soak shows entries opening within minutes of a surge-resume event — the scribe must watch `MARKET_STRESS_RESUMED` events with a surge breadth value and check for near-term fade entries. (Added M23) |
| **[MEDIUM] Differentiated correlated slot-C strategy.** The correlated path is plumbed (slot C, `SlotManager.assignCorrelated`, BTC-correlated buffer in `StrategyService`) but the correlated winner is evaluated by the same `momentumCore`/reversion logic as an idiosyncratic trade. There is no BTC regime classifier, no trend-following entry/exit for slot C, no correlated-specific sizing, no independent backtest. Building slot C before the idiosyncratic path produces attributable fills makes PnL unattributable. **Gate:** M29 14-day soak with measurable expectancy (even negative) on ≥20 closed idiosyncratic trades. **Prerequisite:** M29 idiosyncratic-fill confirmed with ≥20 closed trades across ≥3 trading days + sub-period regime robustness check. **(M30)** The prose floor is now an executable instrument — read the gate from `getIdiosyncraticEdgeReport.slotCGateOpen` (`= meetsClosedTradeFloor AND meetsTradingDayFloor`), not a hand-counted assertion; do not open this entry until that query returns `true`. | `SlotManager`, `StrategyService`, `risk/service/PositionSizer.ts`, `packages/analysis` (`getIdiosyncraticEdgeReport`) | M29 (deferred) | Build the idiosyncratic path first so slot-C PnL is attributable. (Added M29; gate made executable M30) |
| **[MEDIUM] Idiosyncrasy-threshold calibration (evidence-backed).** `idiosyncrasy_min_score = 0.5` is the load-bearing A/B-slot eligibility gate (per-version DB param in the seed migration, shared by all versions). The WIP's premise that the cut is `0.3` is **stale** — the real production cut is `0.5`. M30's `getIdiosyncrasyMissDistribution` gathers the per-day miss-distance histogram for `no_eligible_slot` rejects so a future move starts from measured evidence (are rejects deep BTC-beta near score 0, or marginal misses near 0.4?) rather than a guess. **Any threshold move must start from 0.5, not 0.3**, and is a seed-migration + backtest change (out of M30 scope). Do not loosen the gate under sample pressure — a thin idiosyncratic sample is a reason to extend the soak, never to lower the cut. | `apps/engine/src/database/migrations/20260522020000-SeedStrategyVersions.ts`, `packages/analysis` (`getIdiosyncrasyMissDistribution`) | M30 | Evidence-gathering shipped; the threshold move is a separate milestone. (Added M30) |
| **[MEDIUM] Unify live/backtest idiosyncrasy scoring.** The live indicator (`computeIdiosyncrasyScore.ts`) computes `1 − abs(btc)/abs(coin)`; `BacktestEventBuilder.ts` has a **separate private** formula `abs(symbol−btc)/(abs(symbol)+abs(btc)+0.0001)`. They yield different scores for the same inputs, so the same strategy sees a different idiosyncrasy gate live vs in backtest — a violation of the same-code invariant on this field. Pre-existing (predates M30); M30 hardened only the live path and **named** the divergence (ADR 0004 §8b.6, D4b) without fixing it. **Fix:** delete the private `BacktestEventBuilder` formula, call the live `computeIdiosyncrasyScore`, add a live-vs-backtest parity test, and re-baseline affected backtests. Out of M30 scope (touching the backtest formula changes replay outputs). | `apps/engine/src/backtest/service/BacktestEventBuilder.ts`, `apps/engine/src/market-data/indicator/computeIdiosyncrasyScore.ts` | M30 | Requires backtest re-baseline + parity suite. (Added M30) |
| **[MEDIUM] `sl_outside_liquidation` forensics under VWAP structural stop.** `sl_outside_liquidation` is the #1 reachable funnel blocker (66 rejects in the M29 soak window). The gate does **NOT** reject for "stop too far" — it tightens and approves a correct-side wide stop. It rejects only on: wrong-side stop (`isWrongSideStop`), invalid/over-max leverage, or non-positive liquidation fraction. Under v2 momentum (VWAP structural stop), the 66 rejects are most plausibly wrong-side VWAP geometry or over-leverage, not stop distance. **Resolution:** post-M29 decision-row forensics (entry vs stop vs side on rejected rows, using the `getFunnelSummary` sl sub-cause split). Any future milestone touching the VWAP stop or liquidation buffer **MUST** run these forensics first — do not hand-edit stop distance without knowing which sub-cause dominates. | `RiskGateService`, `packages/analysis` (`getFunnelSummary`) | M29 (diagnosed) | Forensics pass required before any "change the stop" milestone. (Added M29) |
| **[MEDIUM] §concurrent-double-close — race on PENDING_OPEN/OPEN flat guard.** The flat-row guard in `applyReduceFillToPosition` is best-effort. Two intents that both read the row at `qty > 0` before either writes can both proceed to a close (monitor-breach-vs-strategy-close cross-path race). The `breachInFlight` dedup in `LocalProtectiveMonitor` covers monitor-vs-monitor. The residual window is documented here; a row-level lock or per-position in-flight set in `applyReduceFillToPosition` is the structural fix. | `apps/engine/src/execution/service/ExecutionService.ts` (`applyReduceFillToPosition`), `LocalProtectiveMonitor` | M31 | Logged M31; fix in M32+. |
| **[MEDIUM] ADD / partial-reduce `open_exposure` recompute gap.** `RiskStateLifecycleListener` only recomputes on `POSITION_OPENED_EVENT` / `POSITION_CLOSED_EVENT`. An ADD increases `qty` (and therefore residual notional) without firing either event — `open_exposure` will understate between an ADD and the next lifecycle event. A partial reduce similarly overstates until the terminal close. Acceptable for M31 (paper soak runs full opens/closes). Fix: fire a recompute on the ADD and partial-reduce paths in M32 (or emit a dedicated lifecycle event from those paths). | `apps/engine/src/risk/listener/RiskStateLifecycleListener.ts` | M31 | Logged M31; fix in M32. |
| **[MEDIUM] `decisions.position_id` not linked (pre-existing gap).** No live writer stamps `decisions.position_id` from the execution path. The entity column exists but is never populated during normal fills. Requires threading the eventId→positionId link through decision capture. Confirmed pre-existing (not caused by the M31 zombie fix). Out of scope for M31. | `DecisionEntity`, execution path | M31 | Logged M31; fix in a future milestone. |
| **[MEDIUM] `persistHalt` residual race — stale full-row upsert clobber.** `RiskGateService.persistHalt` still uses the full-row `upsertDay`, which includes `realizedPnlDay`/`openExposure`/`tradesCount` from the snapshot loaded at `loadState`. If `RiskStateLifecycleListener` recomputes fresh accounting between `loadState` and `persistHalt` (during the awaited calls in between), `persistHalt` can overwrite the fresh values with stale ones. Residual from the M31 halt-clobber fix: the listener side was made column-scoped, but the halt side was not. Acceptable at paper-soak cadence. Fix: make `persistHalt` use a halt-only column-scoped upsert. | `apps/engine/src/risk/service/RiskGateService.ts` (`persistHalt`) | M31 | Logged M31; fix in M32. |
| **[LOW] Persist `stress_rehalt_count`** | `RiskGateService` | M23 | The per-day re-halt cap counter is in-memory, so a mid-day engine restart resets the chatter cap (more-permissive quirk; reviewed, accepted, documented in M23 plan). No migration-free home exists today; revisit alongside the `risk_state.updated_at`/metadata work (M11 TODO). (Added M23) |
| `HaltSourceEnum.PAPER_DRAWDOWN` / `PAPER_RECONCILIATION_DRIFT` dedicated values | `packages/shared/` | M11a | Shared change deferred M15 |
| `IExchangeOrderSnapshot` → shared `IOrder` full migration (dual-shape via D2+D14 ports today) | `apps/engine/` | M11a | |
| Simulator-config-hash real source (sentinel today) | `apps/engine/` | M11a | R3.1 |
| Multi-value filter server-side `IN (...)` — currently client-side page-scoped for multi-select | `DecisionRepository.findPage`, `MetricsController` | M11a soak | Extend to accept array of actions/symbols |
| Strategy-comparison UI (walk-forward OOS, bootstrap CIs, per-regime tables, charting) | `apps/dashboard/` | M11 | Blocked pending depth-aware + lowFidelity extension |
| LLM review/QA/scribe agents wired into CI on PRs (phase-2) | `.github/workflows/` | M14 | User explicitly deferred; proposed future pipeline |
| Code coverage threshold gate (non-blocking advisory) | CI config | M14 | Until baseline defined |
| Dependabot/Renovate automation for supply-chain | `.github/` | M14 | |
| Short-position funding-sign boundary test | `RiskGateService` tests | M14 | Optional belt-and-suspenders; math verified M4/M6 |
| SDF idempotency pre-check before LLM call (cost optimization) | `apps/agent/` | M13 | Detect `week_iso` re-fire early |
| `pickTopSymbols` hardcoded to `['BTCUSDT', 'ETHUSDT', 'SOLUSDT']` | `apps/agent/` | M13 | Needs engine per-symbol trade-count on `IPerformanceByVersionView` |
| `assertSharedRunConfig` only checks window equality | `apps/agent/` | M13 | Needs `simulatorConfigHash` + `seed` on `IBacktestReport` |
| 6 ADR 0019 promotion-gate criteria NOT_AVAILABLE (5, 7, 8, 9, 10b, 11) | `apps/agent/`, `apps/engine/` | M13 | Requires engine extensions to `IBacktestReport`/`IPerformanceByVersionView` |
| MCP_ENGINE_CMD realpathSync TOCTOU (spawn block-level symlink race) | `apps/mcp/` | M13 | |
| GRANT CONNECT identifier quoting (Postgres reserved-word safety) | DB migrations | M13 | |
| mcp_reader NOLOGIN-until-rotate (password management policy) | DB role setup | M13 | |
| Missing index on `decisions(position_id, ts)` | DB schema | M13 | Query perf gate |
| `AuthFailureReasonEnum.BAD_SIGNATURE` split (W1.5) | `apps/engine/src/auth/` | M11 | |
| `LiveGateway` `AppConfigService` injection + parser parity test (W2.5) | `apps/engine/` | M11 | |
| `HKDF` cursor sub-key derivation (W1.7) | `apps/engine/` | M11 | |
| `revoked_jti` TTL prune + age-floor (W1.6) | `apps/engine/` | M11 | |

## LOW — Cosmetic / refactor / nit

| Item | File / Location | Origin | Notes |
|------|----------------|--------|-------|
| **[M21] ETH index-shock leg structurally dead in backtest.** `BacktestRunnerService` hard-seeds `eth5mMovePct: 0` because single-symbol replay cannot reconstruct the ETH reference cross-tape. The ETH index-shock leg (`STRESS_ETH_5M_SHOCK_PCT = 2.5`) therefore never fires in any backtest; the 2.5% threshold can only be calibrated from live/soak telemetry. Not a correctness risk (no false halts), but backtest cannot validate or falsify the ETH threshold. Pre-existing limitation, surfaced explicitly by M21 activating the BTC leg. | `apps/engine/src/backtest/service/BacktestRunnerService.ts:593` | M21 | Single-symbol replay constraint; deferred pending live/soak evidence |
| **[LOW] Stamp `decisions.position_id` on successful open fill.** The `position_id` column + `ManyToOne` FK exist on `DecisionEntity`, but `StrategyService.persistDecision` never writes it on the open path, so it is null on all live soak rows. This forces M30's `getIdiosyncraticEdgeReport` to recover the open-decision snapshot via a LATERAL time-join (`strategy_version_id, symbol, ts ≤ opened_at`) instead of a direct FK join. Stamping it on the successful open fill is a small engine change that would simplify the join (and any future position↔decision attribution). D2 does not depend on it. | `apps/engine/src/strategy/service/StrategyService.ts` (`persistDecision`), `DecisionEntity` | M30 | Column exists but is never written; forces the LATERAL join in D2. (Added M30) |
| Extract shared `WsAuthAdapter` teardown helper used by both `LiveGateway.spec.ts` and `LiveGateway.adversarial.spec.ts` | `apps/engine/tests/ws/` | M17 post-ship | DRY — both specs `afterEach` call `adapter.onModuleDestroy()` identically |
| Remove dead `EXCHANGE_TESTNET` env var — superseded by `EXCHANGE_ENV`, currently misleading (retained read-only in EnvironmentVariables.ts:69) | `apps/engine/src/config/EnvironmentVariables.ts` | M17 incident | Cost debugging time today when operator thought it controlled exchange selector |
| Backup/drift alerts reuse `AlertTypeEnum.UNHANDLED_EXCEPTION` — consider dedicated alert type `AlertTypeEnum.BACKUP_FAILURE` or `AlertTypeEnum.DRIFT_DETECTED` | `apps/engine/src/backup/`, `apps/engine/src/alert/` | M17 incident | Separates backup infra alerts from unhandled exceptions |
| Backup probe `assertDirWritable()` could use `flag:'wx'` (exclusive create) IF probe filename made per-run unique (currently per-pid reused across runs); pg_dump-non-zero credential test could inject sentinel password into mock stderr to be non-vacuous | `apps/engine/src/backup/DbBackupScheduler.ts`, `apps/engine/tests/backup/DbBackupScheduler.spec.ts` | M17 hardening | Low-priority polish |
| `buildLibpqEnv` decodeURIComponent on malformed percent-encoded DATABASE_URL can throw unwrapped | `apps/engine/src/backup/DbBackupScheduler.ts` | M17 | Operator-trusted env var; not wrapped in DbBackupFailedException |
| On pipeline rejection while pg_dump running, child process not explicitly killed | `apps/engine/src/backup/DbBackupScheduler.ts` | M17 | stdout teardown self-terminates; LOW risk |
| `emitHaltChanged` has 5 params; needs `IEmitHaltChangedParams` DTO | `HaltService.ts:218` | M11a soak | CC-M3 |
| `writeAudit` anonymous 11-field inline type; needs named `IWriteAuditParams` | `HaltService.ts:323` | M11a soak | CC-M4 |
| `engageHalt` (58 lines) and `resume` (44 lines) exceed size guideline; extract private helpers | `HaltService.ts:106,165` | M11a soak | CC-M5 |
| `utcDateString` duplicated in `HaltService` and `MetricsController`; promote to `common/utils/dateUtils.ts` | `HaltService.ts:455`, `MetricsController.ts` | M11a soak | |
| `applyClientFilter`/`toServerFilter` duplicated in component and spec; extract to `decisionsFilterUtils.ts` | `DecisionsFeed.tsx`, `DecisionsFeed.spec.tsx` | M11a soak | |
| `notePragmaticAutoClear`/`notePragmaticTransition` near-identical; extract `buildSyntheticAudit()` | `HaltService.ts:253–289` | M11a soak | |
| Duplicated `isHalted() ? 'HALTED' : 'RUNNING'` ternary; extract `currentStateLabel()` | `HaltService.ts:106,166` | M11a soak | |
| `wasAlreadyHalted` context-dependent boolean flag arg; rename to `isIdempotent` | `HaltService.ts:218` | M11a soak | |
| Dead methods in `FakeControlAuditRepository` (`findLatest`, `findHistoryPage`) | `HaltService.resume.spec.ts:37` | M11a soak | |
| `RiskHaltStatePortAdapter` barrel export in `risk/service/index.ts` is misleading; add comment | `apps/engine/src/risk/service/index.ts` | M11a soak | ControlModule-only provider |
| `COLUMN_HELP` 70-line JSX constant inline; extract to `decisionsFeedHelpContent.tsx` | `DecisionsFeed.tsx` | M11a soak | |
| MultiSelect inline `onClick` arrow recreates closure; extract as `toggleOpen` useCallback | `multi-select.tsx:80` | M11a soak | |
| Add comment explaining `TOOLTIP_ESTIMATED_HEIGHT = 400` derivation | `tooltip.tsx:18` | M11a soak | |
| `windowDays` Math.floor vs round (cosmetic quant) | `apps/analysis/` | M13 | |
| Paired test for `analysisValidation.ts` + `BacktestCliArgError` own file | `apps/mcp/` tests | M13 | |
| `McpToolErrorKindEnum` as proper TS enum | `apps/mcp/` | M13 | |
| Control-flow spacing mass edit (~30 spots, formatter pass) | `apps/engine/` | M13 | |
| `waitForChild`/`buildRuntime`/`listPositions` function-size refactors | `apps/mcp/` | M13 | |
| `verifyBearer`/`runUnderWallclock`/`runBoundaryGuard` function-size extractions | `apps/agent/` | M13 | |
| arg-count > 2 DTO refactors in agent (other call sites beyond `composeMarkdown`) | `apps/agent/` | M13 | |
| `void param;` suppressions cleanup (3 in `runWeeklyLoop` + `main`) | `apps/agent/` | M13 | |
| ESLint disable noise (5 pragmas in agent) — eliminable via scoped override | `eslint.config.js` | M13 | |
| pino logger redact paths may need broadening as sub-objects appear | `apps/engine/` | M13 | |
| **[M27] Intentional asymmetry: `ISimulatedFillCore.missedReason: string | null` vs `ISimulatedFill.missedReason: MissedReasonEnum | null`** | `packages/shared/src/interface/`, `apps/engine/` | M27 | Core adapter layer (`HistoricalFillAdapter`, `StreamingFillAdapter`) preserve internal implementation values ('timeout', 'no_tick_cached') as strings; only the public `ISimulatedFill` contract (engine-output, DB persistence, analysis) uses the typed enum (`MISSING_TICK_DATA`, `PRICE_NOT_TOUCHED`). Asymmetry is intentional — allows future adapter additions without modifying the public enum. Low-priority future cleanup: consolidate to single typed enum on both interfaces if adapter diversity proves unnecessary. (Added M27) |
| `notePragmaticTransition` clamps + try-block order + `startOfRiskDayMs` init + `lastTransitionAuditId` JSDoc (W2.6) | `HaltService.ts` | M11 | |
| AUTH token TTL comment (W2.8) | `apps/engine/src/auth/` | M11 | |
| Cache-Control on halt/history endpoints (W2.7) | `HaltController.ts` | M11 | |
| pino-pretty dev-arg fallback (W2.9) | `apps/engine/` | M11 | |
| `auth_tokens` table confirmed stateless — `revoked_jti` sweep cosmetic (W1.6 follow-up) | `apps/engine/` | M11 | |
| `BaseRepository` uuid-PK widening (cosmetic + scaling) | `apps/engine/` | M11 | |
| `AlertTypeEnum.UNHANDLED_EXCEPTION` misuse for drift → dedicated `RATE_LIMIT_DRIFT` / `RATE_LIMIT_UNDER_COUNT` shared-enum type | `apps/engine/src/exchange/`, `packages/shared/` | M18 | Semantic confusion only; reuse is benign. Shared enum change. |
| Real-429 hardening (batch/space the 100-symbol OI & funding bursts) | `apps/engine/src/exchange/` | M18 | Worst-case load ~10%; headroom sufficient, deferred. |
