# Analysis — Hypothesis Registry

Quantitative investigations into strategy parameters, signal quality, and exit mechanics. This registry records every hypothesis tested, its verdict, and what it rules out. **Future sessions MUST consult this before proposing parameter changes or shadow tests** — many dead ends are documented here.

## How to read this

**Verdict categories:**

- **REJECTED:** The hypothesis is clearly falsified by the data (backtest sweep, soak analysis, or shadow cohort). The "Rules out" section lists what this finding prevents re-investigating.
- **SUPPORTED:** The hypothesis is confirmed; the finding was promoted to a milestone plan or became active in live/shadow.
- **INCONCLUSIVE / SEEDING:** The hypothesis is correctly diagnosed but not yet tested with a treatment. Used to seed follow-on investigation.

**"Rules out"** statements are precise and negative: they describe what NOT to try, and why. They are the most valuable output for avoiding re-investigation of proven dead ends.

## Rejected hypotheses (do not re-investigate without new evidence)

| ID | Date | Hypothesis | Method | Key finding | Rules out | Doc |
|---|---|---|---|---|---|---|
| EXP-001 | 2026-06-23 | Widening time-stop horizon (15/30/45/60 min) converts time-stop exits to take-profits, improving win rate | Backtest sweep, v3, 2026-06-09 → 2026-06-24 | At 15 vs 30 min (same trade set): widening from 15 to 30 min degrades expectancy (−$324.74 → −$341.80). All horizons show time-stop dominance (79% at 15 min → 50% at 60 min), but stop-losses outnumber take-profits at every horizon. Win rate is the binding constraint (22–31%), not the exit clock. | Do not widen or disable the 15-min live time stop. Any "give it more time" approach to fixing time-stop trades is futile; the problem is entry selectivity (fewer losers-in-waiting), not the exit horizon. | [`20260623-1158-timestop-sweep.md`](20260623-1158-timestop-sweep.md) |
| EXP-002 | 2026-06-23 | Forcing higher TP:SL ratio (0.5 → 2.0, risk-sized) improves expected value per trade | Backtest sweep, v3, 2026-06-09 → 2026-06-24, risk-size position per new stop | Realized RR rises 0.76 → 1.23; drawdown falls 31% → 28%. But win rate falls in lockstep (22.2% → 19.1%); per-trade expectancy stays negative at every ratio (−1.35 to −1.42). At R:R 1.23, breakeven needs ~45% win rate; the sweep delivers 19%. The gap is enormous. Net-PnL improvement is a confound (trade count shifts 243 → 209), not edge. | Do not re-tune the TP:SL ratio for profitability. Win rate is the binding constraint; adjusting geometry alone cannot fix negative expectancy. Sizer/stop mismatch is separable and worth fixing independently. | [`20260623-1611-rr-sweep.md`](20260623-1611-rr-sweep.md) |
| EXP-004 | 2026-06-29 | Raising `max_tp_dist_factor` from 5.0 to 7.0 recovers ~4.0 orderly `trend_initiation` signals/day that the 5.0 cap was rejecting, improving win rate and profitability | Backtest comparison: v16 (factor=5.0) vs v19 (factor=7.0), 2026-05-30 → 2026-06-29, ~300 symbols, $1000 capital; plus shadow cohort analysis | Backtest: v19 adds 72 trades (+21%) at identical 23.4% win rate. All extra trades are tier2-dominated (34 trades at 11.1% WR = −$115 incremental loss). Net PnL worsens −$535.69 → −$671.57 (−$136). Root cause: `trend_initiation` momentum initiates correctly but price doesn't sustain to 1.5R within the hold window; 82% close on time_stop in both versions, showing momentum doesn't reach TP. Same structural issue as EXP-001 (time-stop dominance at 79–82%). | Do not raise `max_tp_dist_factor` to recover trade volume. The problem is not the cap width; it is that momentum signals don't sustain. The "extra 4.0 signals/day" all lose money when executed (time-stop dominance persists at 7.0). Any future "raise the cap" proposal must read this backtest and EXP-001 first. | [`20260629-max-tp-dist-factor-shadow.md`](20260629-max-tp-dist-factor-shadow.md) |
| EXP-010 | 2026-06-29 | The `forced_exhaustion` mean-reversion (fade) path has better edge than the `trend_initiation` momentum (follow) path studied in EXP-001–009 | Live + shadow flow-type comparison (backtest cannot validate — harness emits 100% `trend_initiation`); `positions`/`decisions`/`shadow_decisions`, v3+v16 | `forced_exhaustion` is **worse, not better**: live WR 10.3% pooled (n=39) / 11.4% in v3 (n=35) vs `trend_initiation` 24.1% / 27.1%; loses in both versions carrying both flows; shadow ~3% TP vs ~4%. Highest time-stop dominance (69.2% vs 55.2%) and worst expectancy (−$0.64 vs −$0.41/trade). **Not a volume problem** — v16 routes ~14 signals/day at a 28.8% open-rate (higher than momentum's 18.5%); the path is fully wired and simply loses. Breakeven at min_rr 1.5 is ~40% WR; fade delivers 10%. The half-retrace VWAP TP + tight slCap stop on a counter-trend entry expires at time-stop near flat (27/39). | Do not reframe the strategy around mean-reversion or invert v3 to fade by default; the VWAP-deviation trigger lacks edge in **both** directions, so direction is not the lever (binding constraint stays entry/regime selectivity). Do not treat `forced_exhaustion` as volume-starved or blame its gating. **Backtest data-integrity bug FIXED 2026-06-29** — root cause: `buildOiIndex` keyed OI by raw ms timestamps; `resolveOpenInterestAt` searched 5m bar boundaries → keys never matched → `oiChange5mPct` always 0 → 0 `forced_exhaustion` emitted. Fixed in `BacktestRunnerService.ts` (binary search on sorted `IOiEntry[]`); see `tech-debt.md M25`. EXP-001–009 absolute WRs computed on broken harness; re-run recommended before acting on any flow-type split. | [`20260629-mean-reversion-forced-exhaustion-exp010.md`](20260629-mean-reversion-forced-exhaustion-exp010.md) |

## Open / inconclusive (seeding follow-on work)

| ID | Date | Hypothesis | Status | Doc |
|---|---|---|---|---|
| EXP-003 | 2026-06-23 | Decomposing live win-rate gap reveals actionable levers (flow_type selectivity, idiosyncrasy gating, signal_score flooring, entry timing) | INCONCLUSIVE — correctly diagnoses that win rate (25–29% live vs 19–22% backtest) is the binding constraint, and identifies signal-quality (flow_type, idiosyncrasy, signal_score) as candidate levers. Synthesis of EXP-001 + EXP-002 + n=188 live decomposition. Tier-A selectivity gains (cut `catalyst_risk`, raise idiosyncrasy gate to ~0.75) and Tier-B core (concentrate on `trend_initiation + idiosyncrasy ≥ 0.75`) appear promising pooled (44% WR at breakeven, 27 trades) but sub-window validation shows core edge is unstable (28.6% WR window 2, 61.5% window 3, small n=13–14 per window). Tier-C entry-timing lever (pullback entry to reduce MAE) is well-motivated but untested. Do not build a milestone assuming the core is profitable; build to cut proven losers and gather more soak on the core. | Seeded M43/M47 signal-quality work; awaiting follow-on backtest per window and entry-timing variant. | [`20260623-1914-win-rate-improvement-analysis.md`](20260623-1914-win-rate-improvement-analysis.md) |
| EXP-005 | 2026-06-29 | Entry selectivity (regime / tier / side filters) reduces `trend_initiation` time-stop dominance (79%) and its loss | INCONCLUSIVE (lever found, treatment untested) — the time-stop *rate* is structural and immovable (73–82% in every cohort), but the time-stop loss is 81% slippage: the 278 time-stops are near-flat on price (gross-ex-slippage −$0.03/trade) and bleed −$1.25/trade to modelled slippage on a high trade count. Strongest, sub-window-robust lever is **dropping tier2** (31% of trades, 56% of net loss, 11.8% WR, worst in all 3 thirds; corroborated live 0/7 wins). `transitioning` regime is the worst regime (52% of trades, PF 0.183). Slippage reduction (maker entry) is the high-upside untested lever — price-only book is near-breakeven (−$36.55). No cohort reaches net-positive. Absolute WR/net are retroactive-code figures (M47 live only from 2026-06-25); relative rankings are trustworthy. | Seeds EXP-006 (tier2-exclusion backtest) and EXP-007 (maker-entry slippage variant); weakly corroborates EXP-003 signal_score floor (live TP scored 70 vs 48–52 losers). | [`20260629-time-stop-dominance-analysis.md`](20260629-time-stop-dominance-analysis.md) |
| EXP-007 | 2026-06-29 | Switching momentum entry from taker (IOC) to maker (post-only) recovers enough modelled slippage to make the price-positive `tier1 ∩ {trending_down, trending_up}` cohort (n=112) net-positive | INCONCLUSIVE — analytical re-compute on the 350-trade v16 JSON. Cohort baseline net −$74.42, gross-ex-slippage +$26.72, slippage $79.54; breakeven needs 93.6% of slippage recovered. Achievable change is **entry-only** maker (S3: net −$29.35 with zero missed fills, worse with any penalty) — does **not** cross zero. Net-positive only under both-leg maker (S4: +$15.72, +$2.32 at 20% TP-miss, −$12.13 at 50%), but ADR 0005 row 7 forces all reduce/close/flatten exits to REDUCE_MARKET and SL to stop-market → 98/112 exits are structurally taker, so S4 is unachievable. Calibration: actual taker 0.04% (not 0.05%), retail maker +0.02% (brief's −0.02% is VIP-only); slippage, not fees, dominates. Upside is one regime (trending_down) on ~14 TP trades in a single 30-day window. | Do not expect entry-only maker to make the momentum book profitable; do not model maker entry as recovering 100% of slippage (entry leg ≈50% only); do not assume the VIP maker rebate at restricted live size. Seeds EXP-008 (entry-only maker with an empirical fast-mover fill model + slot-hold opportunity cost). | [`20260629-maker-entry-slippage-variant.md`](20260629-maker-entry-slippage-variant.md) |
| EXP-008 | 2026-06-29 | Replacing EXP-007's guessed TP-miss rate with an empirical fast-mover fill model (entry-slippage magnitude as fill-probability proxy) plus a slot-hold opportunity-cost charge makes entry-only maker (S3) a decision-grade case for the `tier1 ∩ {trending_down, trending_up}` cohort (n=112) | INCONCLUSIVE — analytical re-compute on the 350-trade v16 JSON. The fill-model premise fails: backtest `slippageCostUsdt` is a deterministic ~0.30% of notional (stdev 1.9e-05), so it encodes position size, not price velocity — TP-hit and time-stop entry-slip distributions are identical (median est $0.3725 vs $0.3741), and the resulting 21.4% "fast-mover miss" is a notional-size artifact, not a velocity signal. Taken at face value, S3 beats the taker baseline (−$74.42) at every miss rate 0–100% (−$29.35 → −$60.98) but is **net-negative at every miss rate**; best case −$29.35 needs an impossible 0% miss. Slot opportunity cost is immaterial (357-min inter-signal gap; <$0.10 at T=5/15, and the average blocked trade is itself a loser). Best sub-cohort `trending_down × tier1` reaches only −$6.25 even at 0% miss. | Do not use backtest slippage as a fast-mover / fill-probability signal (it is a fixed %-of-notional model); do not expect any realistic TP-miss rate to turn entry-only maker net-positive; do not cite slot opportunity cost either way (negligible vs signal spacing). Closes the execution/slippage lever — the binding constraint is win-rate / entry selectivity (EXP-001/002/003/006). Next lever: re-open the EXP-003 selectivity core with per-sub-window validation; revisit maker entry only as an additive trim once a cohort is net-positive on price. |  [`20260629-maker-entry-fill-model-exp008.md`](20260629-maker-entry-fill-model-exp008.md) |
| EXP-009 | 2026-06-29 | A `signal_score` floor and/or `idiosyncrasy` gate raises win rate to a robust, decision-grade level (>40% WR across sub-windows) | INCONCLUSIVE — analytical sweep on the full live cohort with both fields (n=209: v3 n=195 + v16 n=14). `signal_score` is weakly predictive (TP avg 61.4 vs losers ~55); a floor lifts WR monotonically 23.4% → 36.4% (floor 70) but **never reaches 40%, never turns any cohort net-positive** (best −$52 at floor 65), and the gated 95% Wilson CI [23.4%, 49.6%] has a lower bound below 30% with a small Cohen's h=0.26. `idiosyncrasy` is **flat noise** (WR 22–24% across the entire 0.60–0.85 sweep; avg idio identical across all exit reasons) because the ≥0.7 condition is already enforced upstream in `classifyFlowType` — REJECTED as a standalone lever. Combined gates add nothing over the signal_score floor; `catalyst_risk` exclusion *reverses* EXP-003 (lowers WR 23.4% → 19.6% here). Best gate (signal≥65) beats baseline in 3/3 sub-windows but with wide spread (50/25/31%) and only 1/3 net-positive. | Closes the entry-selectivity lever on current data: idiosyncrasy gating is dead, signal_score flooring caps at ~36% WR (17pp short of 52.8% breakeven) and never goes green. No `signal_score_min` param exists; `idiosyncrasy_min_score` (0.7) already maxed. Together with EXP-006/007/008, the analytical levers are exhausted — next step is a longer same-version forward soak to grow the dataset, not another sweep. | [`20260629-selectivity-signal-score-idiosyncrasy-exp009.md`](20260629-selectivity-signal-score-idiosyncrasy-exp009.md) |
| EXP-017 | 2026-07-03 | Does the ADR 0051 volatility-gated `force_close` same-coin retry recover execution-artifact slots without buying adverse selection? (M52 D3/D4 soak) | INCONCLUSIVE / SEEDING — instrumentation only; no data yet. Paper-only, default-off (`XMOM_FORCE_CLOSE_RETRY`). Attribution via `positions.is_retry_entry`; drift calibration via `positions.force_close_atr_units_drift`. Go/no-go bar (ADR 0051 §6): (a) HARD GATE — retry entries show no materially worse forward returns than volatility-regime-matched attempt-1 controls (naive full-population comparison is a selection-bias confound — use matched controls + the metric-5 counterfactual as decisive); (b) retry fill-acceptance rate materially > 0; (c) counterfactual PnL of retried slots ≥ leaving empty, net slippage; (d) `MOMENTUM_RETRY_MAX_ATR_DRIFT` calibrated from the measured force_close drift distribution (replaces provisional 1.0). Fail (a) → disable (skip wins). Flat-fill-sim caveat: slippage/counterfactual metrics are pipeline-validation until the sim charges book impact. No PnL claim until D4 has the sample. | Seeds M52 D4 closure (keep-on / tighten / disable + calibrated threshold). Do not re-propose next-ranked-coin backfill or sub-24h cadence (both rejected — ADR 0051 §7). | (ADR 0051 §6 / M52 D4) |
| EXP-018 | 2026-07-04 | First 24h post-M52a soak read: is the force_close/retry mechanism working, why are force_closes so frequent, and are the stop-losses improvable? | SEEDING — mechanism CONFIRMED healthy (retry fires reliably post-M52a; 1/4 retries recovered into a live position, 3/4 re-rejected on the same thin coin; guard is protective, 0-duration fee-only rejects). **Root cause of the 67% force_close rate (14/20 opens, but only −$2.96 total, operator-trigger-inflated): the entry geometry and the fill guard share the SAME `min_rr=1.5` threshold with ZERO slack** — momentum arms TP at exactly 1.5R at signal (`tpRebaseEligible=false`, TP frozen), and the fill-acceptance guard (`degenerate_geometry_at_fill`) rejects realized R:R < 1.5, so any adverse entry slippage on a long tips it under and force-closes. Split is razor-sharp: R:R-at-fill < 1.5 → reject (ids 241–245,252,254,257,259), ≥ 1.5 → trade (247,249,253,255,256,258). Traded book ~flat (−$0.95 over 2 TP / 4 SL, RR-saved). Stop-losses full-size; one clear give-back (id 247 MFE +4.76% → round-tripped to a full stop). | Do NOT read this as a retry-mechanism failure or M52a regression (retry works; guard is protective). Do NOT widen the fill guard's `min_rr` (admits the fills it exists to reject) — the lever is the signal-time arm. Seeds: shadow-test (a) arm TP with headroom (1.7–1.8R at signal, keep 1.5 guard) or `tpRebaseEligible=true` for xmom; (b) break-even stop after +1R (guard the EXP-001/002 win-rate trap). Route via `bot-architect` (strategy/execution geometry contract). | [`20260704-m52-force-close-retry-soak-analysis.md`](20260704-m52-force-close-retry-soak-analysis.md) |

## Supported

| ID | Date | Hypothesis | Verdict & key finding | Rules out / next | Doc |
|---|---|---|---|---|---|
| EXP-006 | 2026-06-29 | Excluding `coin_tier == 'tier2'` improves the v16 `trend_initiation` momentum book | SUPPORTED (loss-reduction, **not** a profitability fix) — **re-confirmed with fixed OI harness** (2026-06-29). Fixed-harness run: 208 total trades (vs broken-harness 350; −40% because OI fix now correctly skips catalyst/market-beta signals). Tier1 WR 28.1% vs tier2 17.4% (+10.7 pp gap; original: 28.8% vs 11.8% = +17 pp — direction unchanged). Tier1 net −$152 vs tier2 −$141 (33% of trades, 48% of loss). Sub-window WR 30.4%/26.1%/27.7% — more stable than original. Still net-negative: breakeven 52.8% vs 28.1% delivered. | Do not treat tier2 as salvageable; do not expect its removal to make the strategy green. No tier-exclusion param exists — gating needs a shared-schema + engine change, not config. | [`20260629-tier2-exclusion-backtest.md`](20260629-tier2-exclusion-backtest.md) |

Note: SUPPORTED as a defensive gate; **not yet promoted to a milestone** (tier1-only is still net-negative).

## Methodology notes

- **Backtest calibration gaps apply to all runs:** BTC index-shock candle-vs-rolling-window mismatch (live sees more halts), ETH leg dead in single-symbol replay, modelled fills via `tick_aggregates`. The *relative* ranking across parameters is trustworthy; absolute PnL inherits the gaps.
- **OI-index bug fixed 2026-06-29 (tech-debt M25):** EXP-001–009 were run on the broken harness (all trades classified `trend_initiation`, trade count inflated ~70%). EXP-006 was re-run on the fixed harness and verdict confirmed. EXP-001–005/007–009 are inconclusive/rejected with verdicts unlikely to flip; re-running is low priority unless a new hypothesis specifically requires an accurate flow-type split.
- **Single-window analyses are hypothesis-generating, not decision-grade.** Require confirmation across 2–3 disjoint sub-windows and ≥30 trades per cohort before acting.
- **Shadow sizing caveat:** shadow path sizes off virtual equity per version, not the live `PositionSizer`. Use cohort PnL sign, expectancy sign, and win rate as the verdict; treat absolute dollar magnitude as indicative.
- **Analysis queries read only `shadow_decisions` for counterfactuals, never live `decisions`.** See `packages/analysis/src/query/getPerformance.ts` and `compareVersions.ts` for the canonical performance computation from `simulated_fill` JSONB.
